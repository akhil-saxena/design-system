# Synthesis Summary — JobDash Design System Ingest
<!-- Entry point for gsd-roadmapper. Auto-generated by gsd-doc-synthesizer. -->
<!-- Mode: new | Date: 2026-05-05 -->

---

## Project

Name: JobDash Design System v1.0
Product: JobDash — a job-application tracking web product
Stack: React + TypeScript + Tailwind CSS + CSS custom properties (design tokens)
Repo: ~/Documents/workspace/design-system

---

## Scope

53 sections across 10 groups: Foundation, Layout, Inputs, Surfaces, Data Display,
Feedback, Navigation, Patterns, Interaction, Sample Screens. Full light and dark
mode. Web-first, responsive. Companion mobile views, email templates, and status
pages are design references — not React component library deliverables.

---

## Goals

1. Port all design tokens as CSS custom properties (:root + body.dark).
2. Build every missing component to pixel-precise spec.
3. Achieve WCAG 2.2 AAA on all body text; AA-or-better on all UI elements.
4. Pass axe-core scan with zero violations on every component.
5. Pass Sample Screens integration test in both light and dark mode.

---

## Already Shipped (35+ components, not re-scoped by this ingest)

Per project memory: 35 of 53 primitives are already implemented. The exact list
was not re-audited in this ingest. Components explicitly confirmed NOT missing
in this ingest's PRD source include the foundation components listed in the
README build order: Button, Input, Select, Checkbox, Toggle, Badge, Chip, Card,
Modal, Toast, AppShell, AppBar, Sidebar, Footer.

---

## Missing Components — Primary Build Targets This Cycle

The following components are confirmed unimplemented and are the direct output
of this ingest:

  ColorPicker          — full picker + inline color-input field variant
  TypeToConfirm        — type-to-confirm gate for irreversible actions
  ConfirmDialog        — 4-tone confirmation dialog (danger/warn/success/neutral)
  DataGrid             — sortable, resizable, selectable table with bulk actions
  Sparkline            — pure SVG polyline mini-chart
  MiniDonut            — SVG ring chart with animated arc
  MiniBar              — CSS flex bar chart
  RelativeTime         — formatted relative timestamp with hover tooltip
  StatCard             — composition: label + value + trend badge + Sparkline
  AlertBanner          — 4-tone dismissible alert strip
  CommandPalette       — Cmd+K modal search with grouped results
  Pagination           — full variant + compact variant
  Kbd                  — keyboard shortcut display element

---

## Components Needing Status Verification Before Routing

The following appear in ds-advanced.jsx (SPEC) but were not confirmed as missing
in the project context prompt. Verify against codebase before including in build plan:

  Accordion    Breadcrumbs    Skeleton    EmptyStates    StarRating    ContextMenu

---

## Components Flagged Missing in Project Context — No Spec Classified Yet

The following were named in the project context prompt as missing but no spec
document was included in this ingest. Do NOT route them until specs are ingested:

  TreeItem          — hierarchical nav (ds-navigation.jsx)
  CollapsibleSidebar — full sidebar nav component (ds-navigation.jsx)
  MediaCard         — image/media card with hover overlay (ds-mediacards.jsx)
  StatusPages       — 404/500/maintenance/offline page templates (ds-status.jsx)
  NotificationCenter — notification panel/bell (ds-notifications.jsx)
  FileUploadZone    — drag-and-drop file upload area (ds-patterns.jsx)

Spec files to add to next ingest: ds-mediacards.jsx, ds-status.jsx,
ds-navigation.jsx, ds-notifications.jsx, ds-patterns.jsx.

---

## Design Token System

All tokens are CSS custom properties. Light mode on :root; dark mode on body.dark.
No JS recompute on theme switch — pure CSS cascade.

Color system: cream/ink/amber palette (see constraints.md CONSTRAINT-001 for
all exact values). Vivid variants for charts/badges (static across modes).

Typography: Archivo (display), Inter (body), JetBrains Mono (mono/labels).

Spacing: 12-step scale, 4px increments (--space-1 through --space-16).
Radius: 5 stops — 4 / 8 / 12 / 16 / 999px.
Shadow: 3 stops — subtle / card / floating (plus deep dialog shadow).
Motion: 4 durations (120/180/240/360ms) × 3 easings + spring. All respect
  prefers-reduced-motion (cap at 200ms or instant).
Focus: amber ring, box-shadow only (no outline suppression without replacement).

---

## Key Implementation Constraints

- React + TypeScript. No other framework.
- Theming: body.dark class toggle + CSS custom property cascade.
- Always-dark surfaces: hardcoded #1c1917 / #f5f3f0 — never token references.
- Icons: 1.5px stroke, 24x24, stroke="currentColor". Lucide/Heroicons/Phosphor
  acceptable substitute at matching line-weight.
- No raster images in component library. SVG or CSS only.
- ds-* CSS class name protocol must be followed (see constraints.md CONSTRAINT-007).
- axe-core zero-violation gate applies to every component.
- DataGrid depends on Badge, Checkbox, Table, Button, PageButton being present first.

---

## Conflicts Summary

Blockers: 0
Competing variants: 0
Auto-resolved: 3

Full detail: /Users/temp/Documents/workspace/design-system/.planning/INGEST-CONFLICTS.md

Two WARNINGS require user review before routing:
  1. Verify Accordion/Breadcrumbs/Skeleton/EmptyStates/StarRating/ContextMenu
     implementation status in codebase.
  2. Add ds-mediacards.jsx + ds-status.jsx to next ingest before building
     MediaCard, TreeItem, StatusPages.

---

## Intel Files

decisions.md    — 20 design decisions (SPEC/PRD authority, no locked ADRs)
requirements.md — 14 requirements with acceptance criteria
constraints.md  — 14 constraints (token schemas, APIs, NFRs, protocols)
context.md      — 12 context topics (project identity, aesthetics, theming, assets)
SYNTHESIS.md    — this file

---

## STATUS: READY — safe to route (0 blockers, 0 competing variants)

Two warnings are advisory. They do not block routing for the confirmed missing
components listed above.
