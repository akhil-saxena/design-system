---
phase: 018-confirmdialog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/overlays/Modal/index.tsx
  - src/index.ts
autonomous: true
requirements:
  - REQ-18-01
  - REQ-18-02

must_haves:
  truths:
    - "src/overlays/Modal/index.tsx no longer exports ConfirmDialog or ConfirmDialogProps"
    - "src/index.ts no longer imports ConfirmDialog or ConfirmDialogProps from ./overlays/Modal"
    - "Modal itself still compiles and exports correctly (ModalRole, ModalProps, Modal)"
    - "tsc --noEmit passes after removals (no dangling references)"
  artifacts:
    - path: "src/overlays/Modal/index.tsx"
      provides: "Modal component only — ConfirmDialog section removed"
      contains: "export function Modal"
    - path: "src/index.ts"
      provides: "Barrel — Modal exports from ./overlays/Modal without ConfirmDialog"
      contains: "from \"./overlays/Modal\""
  key_links:
    - from: "src/index.ts"
      to: "src/overlays/Modal/index.tsx"
      via: "named export block"
      pattern: "from.*overlays/Modal"
---

<objective>
Remove the old binary-danger ConfirmDialog from src/overlays/Modal/index.tsx and
strip its export from src/index.ts, clearing the way for the new 4-tone
ConfirmDialog to own the name from src/overlays/ConfirmDialog/index.tsx in Plan
018-02.

Purpose: Avoids the "Duplicate export 'ConfirmDialog'" build error that would
occur if both the old and new variants exist in the barrel simultaneously.

Output: Modal/index.tsx contains only Modal. src/index.ts exports Modal-only
from ./overlays/Modal (ConfirmDialog entry moves to ./overlays/ConfirmDialog
in a later plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/018-confirmdialog/018-RESEARCH.md
@.planning/phases/018-confirmdialog/018-PATTERNS.md
</context>

<interfaces>
<!-- Current exports from src/overlays/Modal/index.tsx that must be preserved -->
<!--
export type ModalRole = "dialog" | "alertdialog";
export interface ModalProps { ... }
export function Modal({ ... }: ModalProps) { ... }

Lines 153–237 are the entire ConfirmDialog section (interface + function).
Lines 1–151 are the Modal implementation — leave completely untouched.
-->

<!-- Current src/index.ts lines 40–46 (to be replaced) -->
<!--
export {
  ConfirmDialog,
  Modal,
  type ConfirmDialogProps,
  type ModalProps,
  type ModalRole,
} from "./overlays/Modal";
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove ConfirmDialog section from Modal/index.tsx</name>
  <files>src/overlays/Modal/index.tsx</files>
  <read_first>
    - src/overlays/Modal/index.tsx — read the full file before touching it; lines 153–237 are the section to delete
  </read_first>
  <action>
Delete lines 153–237 from src/overlays/Modal/index.tsx. This is the block that
starts with the comment "// ─── ConfirmDialog - same-file variant export" and
ends with the closing brace of the ConfirmDialog function.

Exactly what to remove:
  - Line 153: // ─── ConfirmDialog - same-file variant export (D-287, D-356) ─────────
  - Lines 155–178: ConfirmDialogProps interface (export interface ConfirmDialogProps)
  - Lines 180–237: ConfirmDialog function (export function ConfirmDialog)

The blank line 152 after the closing brace of Modal can be removed as well.

After the edit, the file ends at line 151 (the closing `}` of the Modal function).
The only exports from this file are: ModalRole, ModalProps, Modal.

No other changes to the Modal implementation — do not touch lines 1–151.

Also update the JSDoc on line 65 (comment "ConfirmDialog ships from this same
file as a named variant export (D-287, D-356)") — remove that parenthetical
sentence since it is no longer accurate. The comment block is lines 45–66;
remove only the sentence on line 65.
  </action>
  <verify>
    <automated>grep -c "ConfirmDialog" /Users/temp/Documents/workspace/design-system/src/overlays/Modal/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ConfirmDialog" src/overlays/Modal/index.tsx returns 0
    - grep -c "export function Modal" src/overlays/Modal/index.tsx returns 1
    - grep -c "export type ModalRole" src/overlays/Modal/index.tsx returns 1
    - File compiles: cd /Users/temp/Documents/workspace/design-system && npx tsc --noEmit 2>&1 | grep "Modal/index" should be empty
  </acceptance_criteria>
  <done>Modal/index.tsx exports only ModalRole, ModalProps, and Modal. Zero references to ConfirmDialog remain in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Update src/index.ts barrel — remove old ConfirmDialog export, keep Modal</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts — read lines 40–46 before editing (the current ./overlays/Modal export block)
  </read_first>
  <action>
Replace lines 40–46 in src/index.ts:

CURRENT (lines 40–46):
  export {
    ConfirmDialog,
    Modal,
    type ConfirmDialogProps,
    type ModalProps,
    type ModalRole,
  } from "./overlays/Modal";

NEW (replace with exactly):
  export {
    Modal,
    type ModalProps,
    type ModalRole,
  } from "./overlays/Modal";

Do NOT add the new ConfirmDialog export yet — Plan 018-04 will add it once the
component file exists. Leave the rest of src/index.ts completely unchanged.
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -A6 'from "./overlays/Modal"' src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "ConfirmDialog" src/index.ts returns 0 lines
    - grep "from \"./overlays/Modal\"" src/index.ts returns exactly 1 match
    - The Modal block contains only: Modal, type ModalProps, type ModalRole
    - cd /Users/temp/Documents/workspace/design-system && npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>src/index.ts barrel no longer exports ConfirmDialog or ConfirmDialogProps. Modal, ModalProps, and ModalRole are still exported cleanly. tsc --noEmit passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline | Compiler validates exported symbols; removing an export without a replacement is a breaking change for any consumer importing ConfirmDialog from the barrel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-018-01-01 | Tampering | src/index.ts barrel | accept | Internal design system with no external consumers pinned to this exact export; replacement export added in Plan 018-04 closes the window |
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd /Users/temp/Documents/workspace/design-system
# 1. No ConfirmDialog reference in Modal source
grep "ConfirmDialog" src/overlays/Modal/index.tsx && echo "FAIL" || echo "PASS"

# 2. No ConfirmDialog in barrel
grep "ConfirmDialog" src/index.ts && echo "FAIL" || echo "PASS"

# 3. TypeScript clean
npx tsc --noEmit && echo "TSC PASS" || echo "TSC FAIL"
```
</verification>

<success_criteria>
- src/overlays/Modal/index.tsx contains zero references to ConfirmDialog
- src/index.ts contains zero references to ConfirmDialog
- npx tsc --noEmit exits 0
- Modal component itself is untouched and still functional
</success_criteria>

<output>
After completion, create `.planning/phases/018-confirmdialog/018-01-SUMMARY.md`
</output>
