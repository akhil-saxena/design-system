---
phase: 018-confirmdialog
plan: 04
type: execute
wave: 4
depends_on:
  - "018-02"
  - "018-03"
files_modified:
  - src/index.ts
autonomous: true
requirements:
  - REQ-18-01
  - REQ-18-02

must_haves:
  truths:
    - "src/index.ts exports ConfirmDialog, TypeToConfirm, ConfirmDialogProps, ConfirmDialogTone, TypeToConfirmProps from ./overlays/ConfirmDialog"
    - "No duplicate export of ConfirmDialog exists in the barrel (old one removed in 018-01)"
    - "tsc --noEmit passes with zero errors"
    - "Full vitest suite passes (no regressions)"
  artifacts:
    - path: "src/index.ts"
      provides: "Barrel with new ConfirmDialog exports from ./overlays/ConfirmDialog"
      contains: "from \"./overlays/ConfirmDialog\""
  key_links:
    - from: "src/index.ts"
      to: "src/overlays/ConfirmDialog/index.tsx"
      via: "named export block"
      pattern: "from.*overlays/ConfirmDialog"
---

<objective>
Wire the new ConfirmDialog and TypeToConfirm into the barrel (src/index.ts) and
run the full test suite + tsc to verify the complete phase gate.

Purpose: Closes Phase 18 by making both components importable from the package
root and confirming no regressions from removals in 018-01 or additions in 018-02.

Output: Updated src/index.ts barrel + green full vitest run + clean tsc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/018-confirmdialog/018-PATTERNS.md
@.planning/phases/018-confirmdialog/018-01-SUMMARY.md
@.planning/phases/018-confirmdialog/018-02-SUMMARY.md
@.planning/phases/018-confirmdialog/018-03-SUMMARY.md
</context>

<interfaces>
<!-- Exports available from src/overlays/ConfirmDialog/index.tsx (created in 018-02) -->
<!--
export type ConfirmDialogTone = "danger" | "warn" | "success" | "neutral";
export interface ConfirmDialogProps { ... }
export function ConfirmDialog(...)
export interface TypeToConfirmProps { ... }
export function TypeToConfirm(...)
-->

<!-- Current Modal block in src/index.ts after 018-01 edit -->
<!--
export {
  Modal,
  type ModalProps,
  type ModalRole,
} from "./overlays/Modal";
-->

<!-- Barrel style reference from src/index.ts (existing BottomSheet block for style guide) -->
<!--
export {
  BottomSheet,
  type BottomSheetHeight,
  type BottomSheetProps,
} from "./overlays/BottomSheet";
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ConfirmDialog + TypeToConfirm barrel exports to src/index.ts</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts — read the full file; locate the Modal export block (currently exporting Modal, ModalProps, ModalRole from ./overlays/Modal); the new ConfirmDialog block goes immediately after it
    - src/overlays/ConfirmDialog/index.tsx — verify exact export names before writing the barrel entry
  </read_first>
  <action>
In src/index.ts, insert a new export block immediately after the ./overlays/Modal
export block.

The Modal block currently looks like (after 018-01):
  export {
    Modal,
    type ModalProps,
    type ModalRole,
  } from "./overlays/Modal";

Insert exactly this block on the line after the closing `} from "./overlays/Modal";`:
  export {
    ConfirmDialog,
    TypeToConfirm,
    type ConfirmDialogProps,
    type ConfirmDialogTone,
    type TypeToConfirmProps,
  } from "./overlays/ConfirmDialog";

All type exports use the inline `type` keyword (matching the existing project style
in every other export block in the barrel file).

Do not move or reorder any other exports. Do not touch any other line.
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -A7 'from "./overlays/ConfirmDialog"' src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "ConfirmDialog" src/index.ts returns exactly the new export block lines (no old Modal/index references)
    - grep "TypeToConfirm" src/index.ts returns the new export block lines
    - grep -c 'from "./overlays/ConfirmDialog"' src/index.ts returns 1
    - grep -c 'from "./overlays/Modal"' src/index.ts returns 1 (Modal block unchanged, just one block)
  </acceptance_criteria>
  <done>src/index.ts barrel exports ConfirmDialog, TypeToConfirm, ConfirmDialogProps, ConfirmDialogTone, TypeToConfirmProps from ./overlays/ConfirmDialog.</done>
</task>

<task type="auto">
  <name>Task 2: Run full tsc + vitest suite — phase gate</name>
  <files></files>
  <read_first>
    - No files to read. This task runs verification commands only.
  </read_first>
  <action>
Run the following commands in sequence and report results:

1. TypeScript check:
   cd /Users/temp/Documents/workspace/design-system && npx tsc --noEmit

   Expected: exits 0 with zero errors. If errors appear, fix them before
   continuing (likely a missing type import or an incorrect export name).

2. Full test suite:
   cd /Users/temp/Documents/workspace/design-system && npx vitest run

   Expected: all tests pass including the 13 new ConfirmDialog tests.
   If any existing tests fail, diagnose whether the 018-01 removal of
   ConfirmDialog from Modal caused the issue and fix accordingly.
   (The Modal.test.tsx file may reference ConfirmDialog — if so, update
   Modal.test.tsx to remove those test cases since the component moved.)

3. ConfirmDialog-only sanity check:
   cd /Users/temp/Documents/workspace/design-system && npx vitest run src/overlays/ConfirmDialog --reporter=verbose

   Expected: all tests in that directory pass, reporter shows individual
   test names and PASS status.

If Modal.test.tsx contains tests for the old ConfirmDialog (which was removed
in 018-01), those tests must be deleted from Modal.test.tsx because the component
no longer lives there. Do not add tests back to Modal.test.tsx — the tests for
the new ConfirmDialog live in ConfirmDialog.test.tsx (created in 018-02).
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0 (all tests pass)
    - npx vitest run src/overlays/ConfirmDialog shows passing tests for ConfirmDialog and TypeToConfirm
    - No "Duplicate export" errors from tsc
  </acceptance_criteria>
  <done>Full vitest suite passes. tsc --noEmit exits 0. Phase 18 is complete and all components are reachable from src/index.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public barrel export | Any symbol exported from src/index.ts becomes part of the package API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-018-04-01 | Tampering | Barrel re-export | accept | Internal design system; no external semver consumers; type safety enforced by tsc |
</threat_model>

<verification>
Phase gate — run after both tasks complete:

```bash
cd /Users/temp/Documents/workspace/design-system

# 1. New exports present in barrel
grep "ConfirmDialog\|TypeToConfirm" src/index.ts

# 2. No duplicate export (old overlays/Modal entry gone)
grep -c "ConfirmDialog" src/index.ts  # should be 3 (ConfirmDialog, type ConfirmDialogProps, type ConfirmDialogTone)

# 3. TypeScript clean
npx tsc --noEmit && echo "TSC PASS"

# 4. Full test suite
npx vitest run && echo "TESTS PASS"
```
</verification>

<success_criteria>
- src/index.ts exports ConfirmDialog, TypeToConfirm, ConfirmDialogProps, ConfirmDialogTone, TypeToConfirmProps from ./overlays/ConfirmDialog
- npx tsc --noEmit exits 0 with zero errors
- npx vitest run exits 0 — all Phase 18 tests plus all prior tests pass
- Phase 18 is shippable: both components importable from "@akhil-saxena/design-system"
</success_criteria>

<output>
After completion, create `.planning/phases/018-confirmdialog/018-04-SUMMARY.md`
</output>
