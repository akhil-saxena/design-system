---
phase: 018-confirmdialog
plan: 02
type: execute
wave: 2
depends_on:
  - "018-01"
files_modified:
  - src/overlays/ConfirmDialog/index.tsx
  - src/overlays/ConfirmDialog/ConfirmDialog.test.tsx
autonomous: true
requirements:
  - REQ-18-01
  - REQ-18-02

must_haves:
  truths:
    - "ConfirmDialog renders with all 4 tones: danger, warn, success, neutral"
    - "Each tone shows the correct icon tint background and icon SVG"
    - "Each tone shows the correct confirm button variant/style"
    - "Panel background is rgba(255,255,255,.97) — never flips dark"
    - "Escape calls onCancel; Enter calls onConfirm (when enabled)"
    - "Focus is trapped inside the panel via useFocusTrap"
    - "TypeToConfirm confirm button disabled until typed value === guardWord (=== case-sensitive, no trim)"
    - "TypeToConfirm confirm button shows var(--red) bg when enabled, var(--ink-5) at 60% opacity when disabled"
    - "All tests pass: vitest run src/overlays/ConfirmDialog"
  artifacts:
    - path: "src/overlays/ConfirmDialog/index.tsx"
      provides: "ConfirmDialog + TypeToConfirm components"
      exports:
        - "ConfirmDialog"
        - "TypeToConfirm"
        - "ConfirmDialogProps"
        - "ConfirmDialogTone"
        - "TypeToConfirmProps"
    - path: "src/overlays/ConfirmDialog/ConfirmDialog.test.tsx"
      provides: "Unit tests covering REQ-18-01 and REQ-18-02"
      contains: "describe(\"ConfirmDialog\""
  key_links:
    - from: "src/overlays/ConfirmDialog/index.tsx"
      to: "src/_internals/DSPortal"
      via: "DSPortal wrapper"
      pattern: "DSPortal"
    - from: "src/overlays/ConfirmDialog/index.tsx"
      to: "src/hooks/useFocusTrap"
      via: "useFocusTrap(panel, open)"
      pattern: "useFocusTrap"
    - from: "src/overlays/ConfirmDialog/index.tsx"
      to: "src/inputs/Button"
      via: "Cancel + Confirm Button components"
      pattern: "Button"
---

<objective>
Implement the new 4-tone ConfirmDialog and TypeToConfirm components at
src/overlays/ConfirmDialog/index.tsx, plus a full test suite.

Purpose: Delivers REQ-18-01 (4-tone confirm dialog) and REQ-18-02 (typed gate
component) with verified behavior — all keyboard, focus, tone-button, and
always-light surface constraints confirmed by tests.

Output:
  - src/overlays/ConfirmDialog/index.tsx — two exported components
  - src/overlays/ConfirmDialog/ConfirmDialog.test.tsx — test suite (13 behaviors)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/018-confirmdialog/018-RESEARCH.md
@.planning/phases/018-confirmdialog/018-PATTERNS.md
@.planning/phases/018-confirmdialog/018-01-SUMMARY.md
</context>

<interfaces>
<!-- Imports available in the codebase -->
<!--
From src/_internals/DSPortal.tsx:
  export function DSPortal({ children }: DSPortalProps): React.ReactPortal | null

From src/hooks/useFocusTrap.ts:
  export function useFocusTrap(node: HTMLElement | null, active: boolean): void

From src/inputs/Button/index.tsx:
  export type ButtonVariant = "primary" | "secondary" | "ghost" | "danger"
  export function Button({ variant, size, disabled, style, onClick, children, ...rest }: ButtonProps)

From src/inputs/TextInput/index.tsx:
  export function TextInput({ value, onChange, placeholder, style, ...rest }: TextInputProps)
  // renders <input className="ds-atom-input" />

From src/inputs/Kbd/index.tsx:
  export function Kbd({ children, size }: KbdProps)
  // available sizes: "sm" | "md" (default "md")
-->

<!-- Pattern: callback-ref focus trap (from src/overlays/Modal/index.tsx lines 83–84) -->
<!--
const [panel, setPanel] = useState<HTMLDivElement | null>(null);
useFocusTrap(panel, open);
// ref={setPanel} on panel div — NOT useRef
-->

<!-- Backdrop CSS class available in primitives.css -->
<!--
.ds-atom-modal-backdrop — fixed inset 0, dark scrim, blur(2px), flex centering
  Already used by Modal, Sheet, BottomSheet.
  Reuse — do not create a new backdrop class.
-->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ConfirmDialog + TypeToConfirm in src/overlays/ConfirmDialog/index.tsx</name>
  <files>src/overlays/ConfirmDialog/index.tsx</files>
  <read_first>
    - src/overlays/Modal/index.tsx — exact imports, DSPortal usage, useFocusTrap pattern, biome-ignore comment
    - src/inputs/Button/index.tsx — available variants (primary | secondary | ghost | danger)
    - src/inputs/TextInput/index.tsx — TextInput props API
    - src/inputs/Kbd/index.tsx — Kbd props API (size prop)
    - .planning/phases/018-confirmdialog/018-PATTERNS.md — all code blocks are ready to paste
  </read_first>
  <behavior>
    ConfirmDialog behaviors:
    - Renders null when open=false
    - DSPortal wraps ds-atom-modal-backdrop (biome-ignore on backdrop onClick)
    - Panel: className="ds-atom-confirm-panel", role="alertdialog", aria-modal="true", aria-labelledby, aria-describedby, tabIndex={-1}
    - Panel inline style: width=360, background="rgba(255,255,255,.97)", backdropFilter="blur(14px)", WebkitBackdropFilter="blur(14px)", borderRadius=14, border="1px solid var(--rule)", padding=22, boxShadow="0 16px 48px rgba(0,0,0,.18)"
    - Tone config object drives icon tint bg, icon color, icon SVG (4 SVGs per PATTERNS.md)
    - Confirm button: tone=danger → variant="danger" + style={background:"var(--red)",borderColor:"var(--red)"}; tone=warn → variant="primary" (no extra style); tone=success → variant="primary" + style={background:"var(--amber-d)",borderColor:"var(--amber-d)",color:"#fff"}; tone=neutral → variant="secondary" + style={background:"var(--ink)",borderColor:"var(--ink)",color:"#fff"}
    - Cancel button: always variant="ghost"
    - Keyboard: document-level keydown useEffect (when open); Escape → onCancel(); Enter → onConfirm()
    - Focus trap: useFocusTrap(panel, open) via useState callback-ref pattern
    - Props: open, onClose (alias for onCancel), onConfirm, tone, title, body?, confirmLabel="Confirm", cancelLabel="Cancel"

    TypeToConfirm behaviors:
    - Same DSPortal + backdrop + always-light panel structure as ConfirmDialog
    - State: const [v, setV] = useState(""); const ok = v === guardWord; (NO trim)
    - guardWord prop defaults to "DELETE"
    - Hint row: fontSize=11, color="var(--ink-3)", marginBottom=6 — "Type <Kbd size="sm">{guardWord}</Kbd> to confirm"
    - TextInput: value={v}, onChange={e => setV(e.target.value)}, placeholder={`Type ${guardWord}`}, style={{width:"100%",marginBottom:14}}
    - Confirm button: variant="danger", disabled={!ok}, style when disabled: {background:"var(--ink-5)",opacity:0.6,borderColor:"transparent"}; style when enabled: {background:"var(--red)",borderColor:"transparent"}
    - Keyboard: document-level keydown; Escape → onCancel(); Enter (only when ok===true) → onConfirm()
    - Props: open, onClose (alias for onCancel), onConfirm, title, body?, guardWord="DELETE", confirmLabel="Delete forever", cancelLabel="Cancel"
  </behavior>
  <action>
Create /Users/temp/Documents/workspace/design-system/src/overlays/ConfirmDialog/index.tsx.

File structure:
1. Imports: CSSProperties, ReactNode, useEffect, useId, useState from "react";
   DSPortal from "../../_internals/DSPortal";
   useFocusTrap from "../../hooks/useFocusTrap";
   Button, ButtonVariant from "../../inputs/Button";
   TextInput from "../../inputs/TextInput";
   Kbd from "../../inputs/Kbd";
   type { MouseEvent as ReactMouseEvent } from "react"

2. Type: export type ConfirmDialogTone = "danger" | "warn" | "success" | "neutral"

3. Tone config const: tones object — copy verbatim from PATTERNS.md (icon area
   section), preserving exact SVG path data, color strings, and bg values:
   - danger: color="var(--red)", bg="rgba(239,68,68,.1)", triangle-warning SVG
   - warn: color="var(--amber-d)", bg="rgba(245,158,11,.12)", circle-info SVG
   - success: color="var(--green)", bg="rgba(34,197,94,.1)", checkmark SVG
   - neutral: color="var(--ink)", bg="rgba(0,0,0,.05)", circle-info-variant SVG

4. toneButtonStyle const mapping (Record<ConfirmDialogTone, {variant: ButtonVariant; style?: CSSProperties}>):
   - danger:  { variant: "danger",    style: { background: "var(--red)", borderColor: "var(--red)" } }
   - warn:    { variant: "primary",   style: undefined }
   - success: { variant: "primary",   style: { background: "var(--amber-d)", borderColor: "var(--amber-d)", color: "#fff" } }
   - neutral: { variant: "secondary", style: { background: "var(--ink)", borderColor: "var(--ink)", color: "#fff" } }

5. panelStyle const (CSSProperties): width 360, rgba(255,255,255,.97) bg,
   backdropFilter blur(14px), WebkitBackdropFilter blur(14px), borderRadius 14,
   border "1px solid var(--rule)", padding 22, boxShadow "0 16px 48px rgba(0,0,0,.18)"

6. Export interface ConfirmDialogProps: open, onClose, onConfirm, tone, title,
   body?, confirmLabel="Confirm", cancelLabel="Cancel"

7. Export function ConfirmDialog — follow PATTERNS.md section exactly:
   - useState for panel (callback-ref), useId for titleId/descId
   - useFocusTrap(panel, open)
   - useEffect for keyboard (document keydown: Escape→onClose, Enter→onConfirm)
   - if (!open) return null
   - handleBackdropClick: does NOT close (closeOnBackdropClick=false for all tones)
   - JSX: DSPortal > biome-ignore backdrop div > panel div with panelStyle

   Layout inside panel:
   a) Header row: flex, gap=14, marginBottom=12
      - Icon area: 40×40, borderRadius=10, bg=t.bg, color=t.color, flex center, {t.icon}
      - Text block: flex=1, paddingTop=2
        - Title: id={titleId}, fontFamily="var(--display)", fontWeight=700, fontSize=15, marginBottom=5
        - Body: id={descId}, fontSize=12.5, color="var(--ink-2)", lineHeight=1.5
   b) Footer: flex, justifyContent="flex-end", gap=8, marginTop=18
      - Button variant="ghost" onClick={onClose}: {cancelLabel}
      - Button variant={btnConfig.variant} style={btnConfig.style} onClick={onConfirm}: {confirmLabel}

8. Export interface TypeToConfirmProps: open, onClose, onConfirm, title, body?,
   guardWord="DELETE", confirmLabel="Delete forever", cancelLabel="Cancel"

9. Export function TypeToConfirm — same outer structure as ConfirmDialog but:
   - Add: const [v, setV] = useState(""); const ok = v === guardWord
   - Keyboard useEffect: Enter handler checks ok before calling onConfirm
   - Body section replaces the tone icon area with:
     - Title (Archivo 700 15px, no icon area)
     - Body text if provided
     - Hint row: fontSize=11, color="var(--ink-3)", marginBottom=6,
       inline text "Type " + <Kbd size="sm">{guardWord}</Kbd> + " to confirm"
     - TextInput: value={v}, onChange={e=>setV(e.target.value)},
       placeholder={`Type ${guardWord}`}, style={{width:"100%",marginBottom:14}}
   - Footer confirm button: variant="danger", disabled={!ok},
     style={!ok ? {background:"var(--ink-5)",opacity:0.6,borderColor:"transparent"}
                 : {background:"var(--red)",borderColor:"transparent"}}

Do NOT use forwardRef (matches all other overlay components in the codebase).
Do NOT use var(--cream) anywhere — always rgba(255,255,255,.97) for panel bg.
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && npx tsc --noEmit 2>&1 | grep "ConfirmDialog" && grep -c "var(--cream)" src/overlays/ConfirmDialog/index.tsx | grep "^0$" || echo "CONSTRAINT-010 FAIL: var(--cream) found in component"</automated>
  </verify>
  <acceptance_criteria>
    - src/overlays/ConfirmDialog/index.tsx contains "rgba(255,255,255,.97)"
    - grep -c "var(--cream)" src/overlays/ConfirmDialog/index.tsx returns 0 (CONSTRAINT-010: always-light surface must never use cream token)
    - src/overlays/ConfirmDialog/index.tsx exports ConfirmDialog and TypeToConfirm
  </acceptance_criteria>
  <done>src/overlays/ConfirmDialog/index.tsx exists, exports ConfirmDialog and TypeToConfirm, compiles with tsc --noEmit (zero errors in that file), and contains zero var(--cream) references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write ConfirmDialog.test.tsx covering all 13 REQ behaviors</name>
  <files>src/overlays/ConfirmDialog/ConfirmDialog.test.tsx</files>
  <read_first>
    - src/overlays/Modal/Modal.test.tsx — exact test structure, portal query pattern, keyboard event pattern
    - src/inputs/Kbd/Kbd.test.tsx — Phase 17 test style reference
    - .planning/phases/018-confirmdialog/018-PATTERNS.md — TypeToConfirm test patterns section
    - src/overlays/ConfirmDialog/index.tsx — (just written) verify export names and prop names
  </read_first>
  <behavior>
    ConfirmDialog describe block:
    - "renders null when open=false" — render with open=false, panel = document.body.querySelector(".ds-atom-confirm-panel"), expect(panel).toBeNull()
    - "renders panel with role alertdialog when open=true" — panel not null, role="alertdialog", aria-modal="true"
    - "calls onClose on Escape keydown" — vi.fn(), fireEvent.keyDown(document, {key:"Escape"}), called once
    - "calls onConfirm on Enter keydown" — vi.fn(), fireEvent.keyDown(document, {key:"Enter"}), called once
    - "backdrop click does NOT call onClose" — render, click backdrop, onClose not called
    - "danger tone: confirm button has data-variant=danger" — screen.getByText(confirmLabel).closest("button").getAttribute("data-variant") === "danger"
    - "warn tone: confirm button has data-variant=primary" — same pattern
    - "success tone: confirm button has data-variant=primary" — same pattern
    - "neutral tone: confirm button has data-variant=secondary" — same pattern
    - "panel background is rgba(255,255,255,.97) regardless of outer context" — panel element, getComputedStyle or panel.style.background check

    TypeToConfirm describe block:
    - "confirm button is disabled until exact match" — render with guardWord="DELETE", button disabled initially; fireEvent.change(input, {target:{value:"DELETE"}}), button not disabled
    - "comparison is case-sensitive — 'delete' does not enable button" — fireEvent.change with "delete", button still disabled
    - "leading space does not count — ' DELETE' does not enable button" — fireEvent.change with " DELETE", button still disabled
    - "Enter fires onConfirm only when ok=true" — before match: Enter pressed, onConfirm not called; after match: Enter pressed, onConfirm called
    - "guardWord prop overrides default DELETE" — render with guardWord="REMOVE", type "REMOVE", button enabled

    Use: import { fireEvent, render, screen } from "@testing-library/react"
    Use: import { describe, expect, it, vi } from "vitest"
    Use: import { ConfirmDialog, TypeToConfirm } from "."

    Portal query pattern (from Modal.test.tsx):
      document.body.querySelector(".ds-atom-confirm-panel")

    Keyboard event pattern (document-level, from Modal.test.tsx):
      fireEvent.keyDown(document, { key: "Escape" })
      fireEvent.keyDown(document, { key: "Enter" })

    Button variant assertion (from PATTERNS.md):
      screen.getByText(label).closest("button")?.getAttribute("data-variant")
  </behavior>
  <action>
Create /Users/temp/Documents/workspace/design-system/src/overlays/ConfirmDialog/ConfirmDialog.test.tsx.

Write exactly two describe blocks: "ConfirmDialog" and "TypeToConfirm".

Each it() block should be self-contained (fresh render, fresh mocks).

The always-light test can check panel.style.background or use a snapshot of the
inline style property — either approach is acceptable as long as it guards against
a future accidental var(--cream) substitution.

Do not import axe-core — the project uses no automated axe assertions in tests
(Storybook a11y addon handles axe; confirmed by Phase 17 test pattern).
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && npx vitest run src/overlays/ConfirmDialog/ConfirmDialog.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in ConfirmDialog.test.tsx pass. vitest run src/overlays/ConfirmDialog exits with code 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → TypeToConfirm state | User types in the text input; value is compared client-side only, never sent to a server |
| Document keydown listener | Global listener; must be removed on unmount to prevent memory leaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-018-02-01 | Information Disclosure | TypeToConfirm input | accept | Input value is compared locally and never transmitted; no PII risk |
| T-018-02-02 | Denial of Service | document keydown listener | mitigate | useEffect cleanup removes listener on unmount and when open flips false |
| T-018-02-03 | Elevation of Privilege | Enter key confirm trigger | mitigate | Enter handler checks !confirmDisabled / ok===guardWord before calling onConfirm |
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd /Users/temp/Documents/workspace/design-system

# 1. Component file exists with both exports
grep -c "export function ConfirmDialog\|export function TypeToConfirm" src/overlays/ConfirmDialog/index.tsx

# 2. No var(--cream) in the component file (always-light constraint)
grep -v "^.*//.*cream" src/overlays/ConfirmDialog/index.tsx | grep "var(--cream)" && echo "FAIL: cream token found" || echo "PASS: no cream token"

# 3. All tests pass
npx vitest run src/overlays/ConfirmDialog --reporter=verbose

# 4. TypeScript clean
npx tsc --noEmit && echo "TSC PASS" || echo "TSC FAIL"
```
</verification>

<success_criteria>
- src/overlays/ConfirmDialog/index.tsx exists and exports ConfirmDialog, TypeToConfirm, ConfirmDialogProps, ConfirmDialogTone, TypeToConfirmProps
- Panel always uses rgba(255,255,255,.97) — zero occurrences of var(--cream) in component
- vitest run src/overlays/ConfirmDialog exits 0 with all tests passing
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/018-confirmdialog/018-02-SUMMARY.md`
</output>
