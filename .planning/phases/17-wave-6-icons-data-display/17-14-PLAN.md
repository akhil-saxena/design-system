---
phase: 17-wave-6-icons-data-display
plan: 14
type: execute
wave: 6
depends_on: ["17-01", "17-02", "17-03", "17-04", "17-05", "17-06", "17-07", "17-08", "17-09", "17-10", "17-11", "17-12", "17-13"]
files_modified:
  - package.json
  - CHANGELOG.md
  - tests/visual-baselines/
autonomous: false
requirements:
  - DS-60
  - DS-61
  - DS-62
  - DS-63
  - DS-64
  - DS-65
  - DS-66
  - DS-67
  - DS-68
  - DS-69
  - DS-70
tags: [release, version-bump, visual-baselines, changelog]
user_setup: []

must_haves:
  truths:
    - "package.json version bumped from 0.5.6 to 0.6.0"
    - "CHANGELOG.md has a v0.6.0 entry summarizing all 11 DS-NN primitives + lucide refactor + calendarGrid extract"
    - "Cumulative Playwright visual baselines regenerated for the entire library (light + dark for all primitives — old AND new)"
    - "Full test suite is green: npm test && npm run typecheck && npm run check && npm run test:visual"
    - "npm run build succeeds; dist/ contains: index.js, hooks/index.js, icons/index.js, all *.d.ts, three .css files"
    - "tree-shake fixture passes < 5 KB threshold (sanity check)"
    - "DatePicker + DateRangePicker visual baselines remained byte-identical through Plan 02 refactor (no --update-snapshots used on those baselines specifically)"
    - "All 14 lucide-direct importers updated to canonical /icons subpath"
    - "Release commit follows convention: chore(release): v0.6.0 — 11 data display primitives + icons subpath"
  artifacts:
    - path: "package.json"
      provides: "Version bump to 0.6.0"
      contains: "\"version\": \"0.6.0\""
    - path: "CHANGELOG.md"
      provides: "v0.6.0 release notes"
      contains: "## 0.6.0"
    - path: "tests/visual-baselines/"
      provides: "Cumulative regenerated visual baselines for v0.6.0 — light + dark per story across all primitives"
  key_links:
    - from: "package.json"
      to: "git tag (manual after merge)"
      via: "version bump signals release"
      pattern: "0.6.0"
---

<objective>
Wave 6 release — bump version, regenerate cumulative visual baselines, write CHANGELOG, prepare release commit per existing wave-completion pattern (`feat(15-07): Wave 4 v0.4.0 — 6 feedback primitives + cumulative visual baselines`).

This plan **does NOT add new primitive code**. It validates the entire phase output as a coherent v0.6.0 release.

Output: version bump, CHANGELOG entry, regenerated visual baselines for the entire library, final commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-wave-6-icons-data-display/17-CONTEXT.md
@.planning/phases/17-wave-6-icons-data-display/17-RESEARCH.md
@.planning/phases/17-wave-6-icons-data-display/17-PATTERNS.md
@.planning/PROJECT.md
@.planning/STATE.md
@CHANGELOG.md
@package.json
@.planning/phases/17-wave-6-icons-data-display/17-01-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-02-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-03-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-04-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-05-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-06-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-07-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-08-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-09-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-10-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-11-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-12-SUMMARY.md
@.planning/phases/17-wave-6-icons-data-display/17-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final test sweep + version bump + CHANGELOG entry + visual baseline regen</name>
  <files>package.json, CHANGELOG.md, tests/visual-baselines/</files>
  <read_first>
    - All 13 prior plan SUMMARYs in `.planning/phases/17-wave-6-icons-data-display/17-*-SUMMARY.md` (read each — they describe what shipped per plan)
    - CHANGELOG.md (existing format — read entire file to mirror release entry style and section structure)
    - package.json (current version 0.5.6 — bump to 0.6.0)
    - .planning/PROJECT.md "Repository Conventions" (commit format, no Co-Authored-By trailers)
    - 17-CONTEXT.md "Visual baseline regen strategy — cumulative re-baseline at wave-completion commit"
    - 17-RESEARCH.md § "Visual Baseline Cumulative Strategy" lines 686-689 (regen approach)
    - Recent release commits via `git log --grep "chore(release)" --oneline | head -10` (pattern reference)
  </read_first>
  <behavior>
    - Run the full test suite. Every command must exit 0:
      - `npm run test:run` (vitest unit tests)
      - `npm run typecheck` (tsc --noEmit)
      - `npm run check` (biome)
      - `npm run build` (tsup — emits dist/)
      - `npm run test:visual` (Playwright — see note below for baseline regen handling)
    - Bump `package.json` version from `0.5.6` to `0.6.0`.
    - Append a v0.6.0 entry to CHANGELOG.md.
    - Regenerate cumulative visual baselines.
    - Verify dist/ structure after build.
    - Verify tree-shake fixture still passes.
  </behavior>
  <action>
    1. **Pre-flight checks.** Run unit + type + lint + build before any commits:
    ```bash
    npm run test:run
    npm run typecheck
    npm run check
    npm run build
    ```
    All four must exit 0. If anything fails, STOP — fix the failure (or mark a blocker for human review) before proceeding.

    2. **Inspect dist/ structure** after build. Required files:
    ```bash
    ls dist/
    # Expected: index.js, index.d.ts, hooks/, icons/, tokens.css, primitives.css, utilities.css, chunk-*.js
    ls dist/hooks/
    # Expected: index.js, index.d.ts
    ls dist/icons/
    # Expected: index.js, index.d.ts
    ```
    Confirm via `node -e`:
    ```bash
    node -e "const i = require('./dist/index.js'); const newPrims = ['Icon','SegmentedControl','Breadcrumbs','Timeline','InfiniteList','Accordion','Carousel','Tabs','Table','Calendar','RichText']; const missing = newPrims.filter(n => !(n in i)); if (missing.length) { console.error('Missing primitives:', missing); process.exit(1); } console.log('All 11 new primitives present in main barrel');"
    ```

    3. **Verify tree-shake threshold** still met:
    ```bash
    SIZE=$(npx esbuild tests/treeshake/main.ts --bundle --minify --format=esm --external:react --external:react-dom 2>/dev/null | wc -c | tr -d ' ')
    echo "Single-icon bundle size: $SIZE bytes"
    test "$SIZE" -lt 5000
    ```

    4. **Version bump** — edit `package.json`:
    Change:
    ```json
    "version": "0.5.6",
    ```
    To:
    ```json
    "version": "0.6.0",
    ```
    Read the current package.json before editing to confirm the current version (it may have been touched by Plan 00 if any version mod was needed).

    5. **CHANGELOG.md entry.** Append (or insert after the existing top entry — read CHANGELOG to confirm position) a v0.6.0 section. Match the existing prose style. Suggested content:
    ```markdown
    ## 0.6.0 — Wave 6: Data Display Primitives + Canonical Icons

    ### New primitives (DS-60 through DS-70)

    - **Icon** (`DS-60`) — Brand-lock wrapper around lucide-react with built-in size 20, strokeWidth 1.5, `currentColor` defaults. Pre-wrapped icons exported via new subpath `@akhil-saxena/design-system/icons` — tree-shakeable. 14 existing primitives refactored to consume canonical icons; per-callsite drift on stroke-width / size eliminated.
    - **Table** (`DS-61`) — Compound primitive: `Table.Root`, `Table.Header`, `Table.HeaderCell`, `Table.Body`, `Table.Row`, `Table.Cell`, `Table.SelectAllCell`, `Table.SelectCell`, `Table.Pagination`. Helper hooks: `useSortableTable`, `useTableSelection`, `useResizableColumns`. Three densities (`cozy`/`comfortable`/`spacious`), sticky-header opt-in, click-anywhere sort headers, single+multi selection with indeterminate, drag-to-resize with min 60px, paginated nav with truncation algorithm.
    - **Tabs** (`DS-62`) — WAI-ARIA tab pattern with two visual variants (underline default + pill), optional count badges, automatic and manual activation modes, ResizeObserver-driven overflow menu via DSDropdown for narrow viewports.
    - **SegmentedControl** (`DS-63`) — Pill-shaped 2-5 option `radiogroup` with full Arrow/Home/End keyboard model. Three sizes via `data-size`. Used by Calendar's view-mode toggle.
    - **Accordion** (`DS-64`) — WAI-ARIA disclosure pattern (NOT the deprecated tablist accordion). `Accordion.Item` compound member, single + multi-expand modes, configurable heading level (h2-h6), reduced-motion support.
    - **Carousel** (`DS-65`) — Hand-rolled WAI-ARIA carousel with Pointer Events touch swipe, opt-in autoplay (gated on `prefers-reduced-motion`), pause-on-hover/focus, arrow + dot navigation. No external dep (Embla considered, rejected for v0.6).
    - **Timeline** (`DS-66`) — Read-only ordered-list display with `<time>` semantic elements, horizontal + vertical orientations, optional click handlers, dot + connector line via CSS pseudo-elements.
    - **InfiniteList** (`DS-67`) — IntersectionObserver-driven loading sentinel with `hasMore` + `loading` guards, default Skeleton loading slot, end-of-list slot. Virtualization left to consumer (TanStack Virtual recipe documented).
    - **Calendar** (`DS-68`) — Three views (`month`/`week`/`day`) with view-mode toggle via SegmentedControl. Event chips on day cells (max 3 + "+N more" overflow Popover, BottomSheet on mobile breakpoint). `Calendar.Agenda` consumer-rendered slot. Multi-day events render single-day chips per day in range. Today highlight in amber, full dark-mode parity. Built on extracted `_internals/calendarGrid` utility shared with DatePicker.
    - **Breadcrumbs** (`DS-69`) — `<nav>` + `<ol>` semantic with `aria-current="page"` on last item. Truncation collapses middle items into a DSDropdown menu beyond `maxVisible`.
    - **RichText** (`DS-70`) — TipTap-powered headless editor (StarterKit + Link + Placeholder, ~50-70 KB gzipped, externalized in build). Toolbar (Bold/Italic/Underline/Strike/Code/Heading H2-H3/List/OL/Quote/HR/Link) using Button + canonical Icon. Link popover via DSPortal. Three-layer controlled-sync guard prevents the well-known TipTap infinite-loop trap. HTML output by default; JSON via `outputFormat` prop. SSR-safe via `immediatelyRender: false`. Custom toolbar slot.

    ### Internals

    - **calendarGrid utility** (`src/_internals/calendarGrid.ts`) — Pure month-grid math lifted from DatePicker (lines 113-141 in v0.5.x). DatePicker + DateRangePicker refactored to consume; visual baselines remained byte-identical (no `--update-snapshots`). Calendar uses the same utility with `weekStart=1` (Monday-first per handoff).

    ### Hooks (added to `@akhil-saxena/design-system/hooks` subpath)

    - `useReducedMotion` — matchMedia wrapper for prefers-reduced-motion (used by Carousel)
    - `useSortableTable` — pure-derivation sort state for Table
    - `useTableSelection` — single + multi mode selection state with indeterminate
    - `useResizableColumns` — Pointer Events column-resize state with consumer-owned persistence

    ### Build

    - tsup multi-entry: `src/index.ts`, `src/hooks/index.ts`, `src/icons/index.ts` — produces `dist/icons/index.js` + `.d.ts`.
    - `package.json` `exports` extended with `./icons` subpath.
    - `sideEffects: ["*.css"]` declared (preserves CSS imports through tree-shaking).
    - `lucide-react` bumped from `^1.8.0` to `^1.14.0`.
    - New deps: `@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-link`, `@tiptap/extension-placeholder`, `@tiptap/pm` (all at `^3.22.5`, all externalized).

    ### Refactors (no behavior change)

    - 14 primitives migrated from direct `lucide-react` imports to the canonical `Icon` wrapper:
      AlertBanner, Autocomplete, Checkbox, Chip, CopyToClipboard, DatePicker, DateRangePicker, Lightbox, MultiSelect, NumberStepper, Select, SplitButton, StarRating, Toast.
    - `Checkbox` extended with `indeterminate?: boolean` prop (Plan 17-00).

    ### Visual baselines

    - Cumulative regen for v0.6.0 — light + dark stories across all 46 shipped primitives. Pre-existing baselines for DatePicker + DateRangePicker remained byte-identical through the calendarGrid extract (verified before regen).

    ### Out of scope (deferred to v0.7+)

    - Mentions / slash-commands / task-list / code-block-with-syntax-highlighting / collab in RichText.
    - Plain-text-only paste mode.
    - AgendaList chrome expansion in Calendar.
    - Multi-day event spanning bars in Calendar.
    - Year-view in Calendar.
    - Table column reordering.
    - Built-in Table virtualization (consumer brings TanStack Virtual).
    - Grid (responsive card grid wrapper) — explicitly skipped; CSS Grid suffices.
    ```

    Verify CHANGELOG section header style matches existing entries — adjust if the existing file uses a different convention.

    6. **Visual baseline regen** — run Storybook + Playwright to regenerate cumulative baselines.

       Important nuance: Plan 02 (DatePicker refactor) explicitly asserted DatePicker baselines should be byte-identical and NOT regenerated. By the time we reach Plan 14, those baselines are already correct. We're regenerating to:
       - Add baselines for the 11 NEW primitives (Icon stories, Table, Tabs, etc.)
       - Refresh dark-mode stories for any pre-existing primitives whose dark-mode visuals shifted (none should have shifted — but the v0.5.6 hover-specificity fix may have affected DatePicker; verify post-Plan-02 baselines pass first).

       Procedure:
       ```bash
       # Terminal 1:
       npm run storybook
       # Terminal 2 (after Storybook is up at localhost:6006):
       # First: run WITHOUT --update-snapshots to confirm pre-existing baselines pass
       npm run test:visual
       # If pre-existing baselines pass, regenerate to add new baselines:
       npm run test:visual:update
       # Inspect what changed:
       git status tests/visual-baselines/ | head -50
       ```
       Expected `git status`: NEW screenshots added for the 11 new primitives' stories (~50-90 PNG files including dark variants); existing baselines unchanged or only minor refresh from time-sensitive Storybook chrome.

       If pre-existing baselines diff unexpectedly → investigate before --update-snapshots. Do NOT mass-regen if a regression hides in the diff.

    7. **Final full-suite green check:**
    ```bash
    npm run test:run && npm run typecheck && npm run check && npm run build
    ```
    All four exit 0.

    8. **Verify the release artifact** is publishable (DRY-RUN — do NOT actually publish):
    ```bash
    npm pack --dry-run
    ```
    Inspect file list — should include `dist/`, `README.md`, `LICENSE`, `package.json`. No accidental inclusion of `.planning/`, `tests/`, `src/`, `node_modules/`.
  </action>
  <verify>
    <automated>npm run test:run &amp;&amp; npm run typecheck &amp;&amp; npm run check &amp;&amp; npm run build &amp;&amp; node -e "const p = require('./package.json'); if (p.version !== '0.6.0') { console.error('version not 0.6.0:', p.version); process.exit(1); } console.log('version ok:', p.version);" &amp;&amp; grep -q "## 0.6.0" CHANGELOG.md &amp;&amp; node -e "const i = require('./dist/index.js'); const newPrims = ['Icon','SegmentedControl','Breadcrumbs','Timeline','InfiniteList','Accordion','Carousel','Tabs','Table','Calendar','RichText']; const missing = newPrims.filter(n =&gt; !(n in i)); if (missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All primitives present');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').version)"` prints `0.6.0`
    - `grep -c "^## 0.6.0" CHANGELOG.md` returns at least 1
    - `npm run test:run` exits 0
    - `npm run typecheck` exits 0
    - `npm run check` exits 0
    - `npm run build` exits 0; `dist/icons/index.js`, `dist/hooks/index.js`, `dist/index.js`, three .css files all present
    - All 11 new primitives importable from main barrel: Icon, SegmentedControl, Breadcrumbs, Timeline, InfiniteList, Accordion, Carousel, Tabs, Table, Calendar, RichText
    - Tree-shake fixture: `npx esbuild tests/treeshake/main.ts --bundle --minify --format=esm --external:react --external:react-dom | wc -c` returns < 5000
    - `git status tests/visual-baselines/ | wc -l` shows new PNG files added for new primitives
    - `npm pack --dry-run` lists `dist/`, `README.md`, `LICENSE`, `package.json` with no spurious inclusions
  </acceptance_criteria>
  <done>v0.6.0 release artifacts ready: version bumped, CHANGELOG written, baselines regenerated, full suite green, dist/ structure verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: v0.6.0 release commit + tag</name>
  <what-built>
    - 11 new primitives (DS-60..70) shipping at v0.6.0
    - lucide refactor sweep (14 primitives migrated to canonical Icon wrapper)
    - calendarGrid utility extract (DatePicker visual-byte-identical)
    - 4 new helper hooks (useReducedMotion, useSortableTable, useTableSelection, useResizableColumns)
    - tsup multi-entry build with /icons subpath
    - Cumulative visual baselines regenerated for the entire library
    - CHANGELOG.md v0.6.0 entry
    - package.json bumped to 0.6.0
  </what-built>
  <how-to-verify>
    1. Run a final sanity sweep: `npm run test:run && npm run typecheck && npm run check && npm run build`. All exit 0.
    2. Inspect Storybook visually: `npm run storybook`. For each new primitive (11 total), verify:
       - Light + dark renders match handoff expectations
       - Keyboard navigation works (Tab/Arrow keys per ARIA pattern)
       - No console errors on story load
    3. Verify the 14 lucide-refactored primitives still look identical to v0.5.6 (visual smoke check on Storybook):
       - Open AlertBanner, Autocomplete, Checkbox, Chip, CopyToClipboard, DatePicker, DateRangePicker, Lightbox, MultiSelect, NumberStepper, Select, SplitButton, StarRating, Toast
       - Confirm icons render with same visual weight (lucide v1.14 vs v1.8 + brand-lock wrapper should be visually identical at the consumer-supplied size)
    4. Check `npm pack --dry-run` output is clean.
    5. **Manual**: paste rich content (Word, Google Docs) into a RichText story; confirm sanitization works (no inline styles, no `<font>` tags, no script execution).
    6. **Manual** (optional): Open the page on an iOS device or BrowserStack; smoke test Carousel touch swipe + Calendar mobile day-events BottomSheet.
    7. Once everything looks good, type "approved" — Claude will create the release commit:
       ```
       chore(release): v0.6.0 — 11 data display primitives + icons subpath + cumulative visual baselines
       ```
       Files: package.json, CHANGELOG.md, tests/visual-baselines/ (newly added baselines).
       NO Co-Authored-By trailer (system rule per PROJECT.md).
  </how-to-verify>
  <resume-signal>Type "approved" to commit the v0.6.0 release. Or describe any issue (visual regression, broken story, missing baseline, console error) for repair.</resume-signal>
</task>

</tasks>

<threat_model>
N/A — Release plan operates on artifacts produced by Plans 01-13. Threat models for each primitive are declared in their respective plans.

Cross-plan supply-chain notes (already mitigated in Plan 00):
- TipTap + lucide pinned to ^MAJOR.MINOR — package-lock.json captures exact resolutions for reproducibility
- All TipTap deps externalized — small consumers don't pay for them
</threat_model>

<verification>
- All 11 new primitives importable from main barrel
- /icons subpath builds + tree-shakes
- /hooks subpath builds (4 new hooks added)
- DatePicker + DateRangePicker visual baselines pass without regen (proves Plan 02 refactor was byte-identical)
- Cumulative visual baselines regenerate cleanly
- CHANGELOG follows existing prose style
- npm pack output clean
</verification>

<success_criteria>
1. package.json version = 0.6.0
2. CHANGELOG.md has v0.6.0 entry covering DS-60..70 + refactors + build changes
3. All 11 new primitives exported from main barrel
4. dist/icons/ subpath emitted
5. Cumulative visual baselines regenerated and committed
6. Full test suite green (test, typecheck, check, build)
7. Tree-shake threshold < 5 KB held
8. Release commit pending human approval
</success_criteria>

<output>
After completion, create `.planning/phases/17-wave-6-icons-data-display/17-14-SUMMARY.md` AND update `.planning/STATE.md` (status, last_shipped → v0.6.0, last_anchor_commit → the new release commit hash, progress → 5/6 phases complete).
</output>
