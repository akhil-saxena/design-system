---
phase: 17-wave-6-icons-data-display
plan: 07
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/Accordion.tsx
  - src/Accordion.stories.tsx
  - src/Accordion.test.tsx
  - src/primitives.css
  - src/index.ts
autonomous: true
requirements:
  - DS-64
tags: [primitive, disclosure, aria, accordion]
user_setup: []

must_haves:
  truths:
    - "Accordion renders Items with header buttons + region panels per WAI-ARIA disclosure pattern"
    - "Each header is a <button aria-expanded={open} aria-controls={panelId}> wrapped in an h-tag (configurable level, default h3)"
    - "Each panel is a <div id={panelId} role='region' aria-labelledby={headerId} hidden={!open}>"
    - "Compound API — Accordion + Accordion.Item; consumer composes"
    - "mode='single' (default): opening one item closes others; mode='multi': independent toggle"
    - "Chevron rotates 180deg on expand via CSS data-open attribute"
    - "Enter and Space keys toggle expand/collapse"
    - "Reduced-motion: chevron rotation + panel transition skipped per useReducedMotion"
    - ".ds-atom-accordion CSS block in primitives.css"
  artifacts:
    - path: "src/Accordion.tsx"
      provides: "Accordion + Accordion.Item compound API (DS-64)"
      exports: ["Accordion", "type AccordionProps", "type AccordionItemProps"]
      min_lines: 120
    - path: "src/Accordion.stories.tsx"
      provides: "Stories — Single, Multi, Controlled, CustomHeadingLevel, DisabledItem, DarkMode, ReducedMotion, Playground"
    - path: "src/Accordion.test.tsx"
      provides: "Unit tests — aria-expanded toggle, single-mode auto-close, multi-mode independent, keyboard activation"
      min_lines: 90
  key_links:
    - from: "src/Accordion.tsx"
      to: "src/icons/index.ts"
      via: "ChevronDown for header chevron"
      pattern: "from \"\\./icons\""
    - from: "src/Accordion.tsx"
      to: "src/hooks/useReducedMotion.ts"
      via: "skip chevron rotation + panel transition when reduced-motion preferred"
      pattern: "useReducedMotion"
---

<!-- Shared-barrel note: src/primitives.css and src/index.ts are append-only in this wave.
     Multiple Wave 2-4 plans add CSS blocks + barrel exports concurrently. Each plan APPENDS
     its block + export — no plan modifies another plan's existing entries. If a merge conflict
     occurs in CI, accept both blocks (concat). This convention is consistent with Waves 3-5
     in earlier phases (Phase 14 surfaces, 15 feedback, 16 inputs). -->

<objective>
DS-64 — Accordion primitive built on the WAI-ARIA disclosure pattern (NOT the deprecated tablist accordion antipattern). Compound API: `Accordion` (root, owns mode + state) + `Accordion.Item` (header + content). Single-expand or multi-expand variants.

Purpose: progressive disclosure for FAQs, settings sections, and detail-on-demand UIs.

Output: 1 primitive file with 2 components + stories + tests, CSS block, barrel export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-wave-6-icons-data-display/17-CONTEXT.md
@.planning/phases/17-wave-6-icons-data-display/17-RESEARCH.md
@.planning/phases/17-wave-6-icons-data-display/17-PATTERNS.md
@.planning/phases/17-wave-6-icons-data-display/17-01-SUMMARY.md

# Analog files
@src/AlertBanner.tsx
@src/InlineConfirm.tsx
@src/Select.tsx
@design_handoff/design-system/ds-accordion.jsx

<interfaces>
```ts
export interface AccordionProps {
  mode?: "single" | "multi"; // default "single"
  defaultOpenIds?: string[];
  openIds?: string[]; // controlled
  onOpenIdsChange?: (ids: string[]) => void;
  children: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
}

export interface AccordionItemProps {
  id: string;
  title: React.ReactNode;
  headingLevel?: 2 | 3 | 4 | 5 | 6; // default 3
  disabled?: boolean;
  children: React.ReactNode;
}

// Compound usage:
// <Accordion mode="single">
//   <Accordion.Item id="faq-1" title="What is X?">Content...</Accordion.Item>
//   <Accordion.Item id="faq-2" title="How does Y work?">Content...</Accordion.Item>
// </Accordion>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Accordion + Accordion.Item compound primitive + stories + tests + CSS + barrel</name>
  <files>src/Accordion.tsx, src/Accordion.stories.tsx, src/Accordion.test.tsx, src/primitives.css, src/index.ts</files>
  <read_first>
    - src/AlertBanner.tsx (forwardRef + data-variant chrome pattern)
    - src/InlineConfirm.tsx (toggle-to-show pattern)
    - src/Select.tsx (chevron rotation pattern via .is-open class — Accordion uses data-open instead per modern data-attr convention)
    - design_handoff/design-system/ds-accordion.jsx (visual reference — header style, chevron position, expand animation)
    - 17-RESEARCH.md § "ARIA Patterns / Accordion" lines 703-710 (disclosure pattern, button + h-tag wrap)
    - 17-PATTERNS.md § "src/Accordion.tsx" lines 277-303 (chrome pattern)
    - 17-PATTERNS.md § "Pattern 5: data-* attribute" — for data-open chevron rotation
    - 17-PATTERNS.md § "Pattern 7: useId for label/describedby"
    - src/hooks/useReducedMotion.ts (Plan 00 — gate panel + chevron transitions)
  </read_first>
  <behavior>
    - Root `Accordion` uses React Context to share mode + open-state with Items.
    - Single mode: opening item closes others. Multi: independent.
    - Controlled via `openIds` + `onOpenIdsChange`; uncontrolled defaults to `defaultOpenIds || []`.
    - `Accordion.Item` renders:
      - `<h{headingLevel} className="ds-atom-accordion-heading">` containing
      - `<button type="button" aria-expanded={open} aria-controls={panelId} id={headerId} disabled={disabled} className="ds-atom-accordion-trigger" data-open={open || undefined}>{title}<Icon icon={ChevronDown} className="ds-atom-accordion-chev" data-open={open || undefined} /></button>`
      - `</h{headingLevel}>`
      - `<div id={panelId} role="region" aria-labelledby={headerId} hidden={!open} className="ds-atom-accordion-panel">{children}</div>`
    - `useId()` generates header + panel IDs.
    - Enter and Space toggle (browsers handle this for free on `<button>`); no extra keydown needed.
    - Reduced-motion: when `useReducedMotion()` returns true, chevron + panel transitions are removed via CSS `.ds-atom-accordion[data-reduced-motion="true"]` selector. Set the data-attribute on the root.
  </behavior>
  <action>
    1. Create `src/Accordion.tsx`. Usage-audit JSDoc per Pattern 8.

    Skeleton:
    ```tsx
    /**
     * # Usage Audit — Accordion (DS-64)
     *
     * Consumers (post v0.6):
     * - FAQ pages, settings sections, doc accordions
     *
     * API:
     * - <Accordion mode="single" | "multi">
     *     <Accordion.Item id="..." title="...">{children}</Accordion.Item>
     *   </Accordion>
     * - Controlled: openIds + onOpenIdsChange
     * - Uncontrolled: defaultOpenIds
     * - headingLevel per Item (2-6, default 3) — important for document outline
     *
     * Implementation:
     * - WAI-ARIA disclosure pattern (NOT tablist accordion antipattern)
     * - <h{N}><button aria-expanded></button></h{N}> + <div role="region">
     * - useReducedMotion gates chevron + panel CSS transitions
     */
    import {
      createContext, forwardRef, useCallback, useContext, useId, useMemo, useState,
    } from "react";
    import { ChevronDown } from "./icons";
    import { useReducedMotion } from "./hooks/useReducedMotion";

    interface AccordionContextValue {
      mode: "single" | "multi";
      openIds: string[];
      toggle: (id: string) => void;
    }
    const AccordionContext = createContext<AccordionContextValue | null>(null);

    export interface AccordionProps {
      mode?: "single" | "multi";
      defaultOpenIds?: string[];
      openIds?: string[];
      onOpenIdsChange?: (ids: string[]) => void;
      children: React.ReactNode;
      className?: string;
      style?: React.CSSProperties;
    }

    const AccordionRoot = forwardRef<HTMLDivElement, AccordionProps>(function Accordion(
      { mode = "single", defaultOpenIds, openIds: controlledOpenIds, onOpenIdsChange, children, className, style },
      ref,
    ) {
      const reducedMotion = useReducedMotion();
      const [uncontrolled, setUncontrolled] = useState<string[]>(defaultOpenIds ?? []);
      const isControlled = controlledOpenIds !== undefined;
      const openIds = isControlled ? controlledOpenIds : uncontrolled;

      const toggle = useCallback((id: string) => {
        const isOpen = openIds.includes(id);
        let next: string[];
        if (mode === "single") {
          next = isOpen ? [] : [id];
        } else {
          next = isOpen ? openIds.filter(x => x !== id) : [...openIds, id];
        }
        if (!isControlled) setUncontrolled(next);
        onOpenIdsChange?.(next);
      }, [openIds, mode, isControlled, onOpenIdsChange]);

      const ctx = useMemo<AccordionContextValue>(() => ({ mode, openIds, toggle }), [mode, openIds, toggle]);

      return (
        <div
          ref={ref}
          className={`ds-atom-accordion${className ? ` ${className}` : ""}`}
          style={style}
          data-reduced-motion={reducedMotion ? "true" : undefined}
        >
          <AccordionContext.Provider value={ctx}>{children}</AccordionContext.Provider>
        </div>
      );
    });

    export interface AccordionItemProps {
      id: string;
      title: React.ReactNode;
      headingLevel?: 2 | 3 | 4 | 5 | 6;
      disabled?: boolean;
      children: React.ReactNode;
    }

    function AccordionItem({ id, title, headingLevel = 3, disabled, children }: AccordionItemProps) {
      const ctx = useContext(AccordionContext);
      if (!ctx) throw new Error("Accordion.Item must be rendered inside <Accordion>");
      const open = ctx.openIds.includes(id);
      const generatedId = useId();
      const headerId = `${generatedId}-h`;
      const panelId = `${generatedId}-p`;

      const Heading = (`h${headingLevel}` as unknown) as keyof JSX.IntrinsicElements;

      return (
        <div className="ds-atom-accordion-item" data-open={open || undefined}>
          <Heading className="ds-atom-accordion-heading">
            <button
              type="button"
              id={headerId}
              aria-expanded={open}
              aria-controls={panelId}
              disabled={disabled}
              onClick={() => ctx.toggle(id)}
              className="ds-atom-accordion-trigger"
              data-open={open || undefined}
            >
              <span className="ds-atom-accordion-title">{title}</span>
              <ChevronDown size={16} className="ds-atom-accordion-chev" data-open={open || undefined} />
            </button>
          </Heading>
          <div id={panelId} role="region" aria-labelledby={headerId} hidden={!open} className="ds-atom-accordion-panel">
            {children}
          </div>
        </div>
      );
    }

    // Attach Item as namespace member of Accordion
    export const Accordion = Object.assign(AccordionRoot, { Item: AccordionItem });
    ```

    2. Create `src/Accordion.stories.tsx`. Stories:
    - `Single` — mode=single (default), 4 items
    - `Multi` — mode=multi, 4 items
    - `Controlled` — controlled openIds via parent state
    - `CustomHeadingLevel` — items with headingLevel=2 (h2)
    - `DisabledItem` — one item disabled
    - `DarkMode`
    - `ReducedMotion` — story with media-query forced via Storybook parameter or wrapper class
    - `Playground`

    3. Create `src/Accordion.test.tsx`. Required tests:
    - renders headingLevel as h-tag (default h3)
    - clicking trigger toggles aria-expanded + hidden
    - single-mode: opening item B closes item A
    - multi-mode: items toggle independently
    - controlled mode: openIds prop wins; onOpenIdsChange fires
    - uncontrolled mode: defaultOpenIds initial state
    - disabled item: clicking is no-op
    - panel has role=region + aria-labelledby pointing to header id
    - chevron has data-open=true when open

    4. **CSS** — append to `src/primitives.css`:
    ```css
    /* ─── DS atom: Accordion (DS-64) ────────────────────────────────────────
       WAI-ARIA disclosure pattern. <h><button></h> + <div role="region">.
       Chevron rotates on expand; reduced-motion skips transition. */
    .ds-atom-accordion {
      display: flex;
      flex-direction: column;
      border: 1px solid var(--rule);
      border-radius: 8px;
      overflow: hidden;
      background: var(--surface);
    }
    .ds-atom-accordion-item + .ds-atom-accordion-item {
      border-top: 1px solid var(--rule);
    }
    .ds-atom-accordion-heading {
      margin: 0;
      padding: 0;
      font-family: var(--font-display);
    }
    .ds-atom-accordion-trigger {
      appearance: none;
      width: 100%;
      background: transparent;
      border: 0;
      padding: 14px 16px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      color: var(--ink);
      font: inherit;
      font-size: 14px;
      font-weight: 600;
      text-align: left;
    }
    .ds-atom-accordion-trigger:hover:not(:disabled) {
      background: var(--surface-2);
    }
    .ds-atom-accordion-trigger:focus-visible {
      outline: 2px solid var(--amber);
      outline-offset: -2px;
    }
    .ds-atom-accordion-trigger:disabled {
      cursor: not-allowed;
      opacity: 0.5;
    }
    .ds-atom-accordion-title {
      flex: 1;
    }
    .ds-atom-accordion-chev {
      transition: transform 200ms ease;
      flex-shrink: 0;
    }
    .ds-atom-accordion-chev[data-open] {
      transform: rotate(180deg);
    }
    .ds-atom-accordion-panel {
      padding: 0 16px 16px;
      color: var(--ink);
      font-family: var(--font-body);
      font-size: 13px;
      line-height: 1.6;
    }
    /* Reduced-motion: skip chevron transition */
    .ds-atom-accordion[data-reduced-motion="true"] .ds-atom-accordion-chev {
      transition: none;
    }
    /* Dark mode */
    :root.dark .ds-atom-accordion {
      background: var(--surface);
      border-color: var(--rule);
    }
    :root.dark .ds-atom-accordion-trigger:hover:not(:disabled) {
      background: var(--surface-2);
    }
    :root.dark .ds-atom-accordion-trigger { color: var(--ink); }
    :root.dark .ds-atom-accordion-panel { color: var(--ink); }
    ```

    5. **Barrel** — append to `src/index.ts`:
    ```ts
    export { Accordion, type AccordionProps, type AccordionItemProps } from "./Accordion";
    ```

    6. Run tests + typecheck.
  </action>
  <verify>
    <automated>npm run test:run -- Accordion &amp;&amp; npm run typecheck &amp;&amp; grep -c "ds-atom-accordion" src/primitives.css &amp;&amp; grep -q "Accordion" src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/Accordion.tsx` and `.stories.tsx` and `.test.tsx` pass
    - `grep -c "aria-expanded" src/Accordion.tsx` returns at least 1
    - `grep -c "role=\"region\"" src/Accordion.tsx` returns at least 1
    - `grep -c "aria-controls" src/Accordion.tsx` returns at least 1
    - `grep -c "useReducedMotion" src/Accordion.tsx` returns at least 1
    - `grep -c "Accordion.Item\\|Object.assign" src/Accordion.tsx` returns at least 1
    - `grep -v '^/\\*\\| \\*' src/primitives.css | grep -c "ds-atom-accordion"` returns at least 8
    - `grep -c ":root.dark .ds-atom-accordion" src/primitives.css` returns at least 1
    - `grep -c "Accordion" src/index.ts` returns at least 1
    - `npm run test:run -- Accordion` exits 0 with at least 9 tests passing
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Accordion compound primitive ships with disclosure pattern, single/multi modes, controlled/uncontrolled, configurable heading level, reduced-motion, dark-mode CSS, public export.</done>
</task>

</tasks>

<threat_model>
N/A — Primitive renders consumer-supplied React nodes via `title` prop and children. Standard JSX text rendering. No HTML string parsing.
</threat_model>

<verification>
- aria-expanded toggles on click
- Single-mode auto-closes siblings
- Reduced-motion attribute set when matchMedia matches
- Panel role=region + aria-labelledby correct
</verification>

<success_criteria>
1. WAI-ARIA disclosure pattern implementation
2. Compound API: Accordion + Accordion.Item
3. Single + multi modes
4. Controlled + uncontrolled modes
5. Configurable heading level (h2-h6)
6. Reduced-motion respected
7. Public export from src/index.ts
8. 9+ unit tests, 8 stories
</success_criteria>

<output>
After completion, create `.planning/phases/17-wave-6-icons-data-display/17-07-SUMMARY.md`.
</output>
