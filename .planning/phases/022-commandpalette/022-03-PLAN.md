---
phase: 022-commandpalette
plan: "03"
type: execute
wave: 3
depends_on:
  - "022-02"
files_modified:
  - src/overlays/CommandPalette/CommandPalette.stories.tsx
  - src/index.ts
autonomous: true
requirements:
  - REQ-22-01

must_haves:
  truths:
    - "CommandPalette.stories.tsx exports at least 4 stories: Default, WithIcons, FilteredEmpty, DarkMode"
    - "src/index.ts exports CommandPalette, CommandPaletteProps, CommandPaletteItem"
    - "Cmd+K listener is shown in story scaffolding (consumer responsibility)"
    - "tsc --noEmit exits 0"
    - "Full vitest suite passes (Phase 22 tests + pre-existing pattern unchanged)"
---

<objective>
Add Storybook stories for CommandPalette, wire barrel export, run final phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: CommandPalette stories</name>
  <files>src/overlays/CommandPalette/CommandPalette.stories.tsx</files>
  <read_first>
    - src/overlays/ConfirmDialog/ConfirmDialog.stories.tsx (overlay stories pattern with Demo wrapper)
    - src/overlays/CommandPalette/index.tsx
  </read_first>
  <action>
Create `src/overlays/CommandPalette/CommandPalette.stories.tsx` following the ConfirmDialog stories pattern. Stories:

- **Default** — `CommandPaletteDemo()` wrapper with useState for open + Cmd+K window listener registered in useEffect (consumer demonstration). Trigger button: "Open Command Palette ⌘K". Items array spans Actions / Recent / Navigation groups (8-10 items total).
- **WithIcons** — Items include lucide-react icons (Plus, Search, Settings, etc.). Demonstrates the icon prop in items.
- **FilteredEmpty** — Pre-set the demo state so the palette opens with a query that yields no matches; demonstrates the empty state UI.
- **DarkMode** — Default demo wrapped in `<div className="dark" style={{ background: "#1c1917", padding: 32, minHeight: "100vh" }}>...`.

Each demo function uses:
```tsx
function CommandPaletteDemo() {
  const [open, setOpen] = useState(false);
  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      if ((e.metaKey || e.ctrlKey) && e.key === "k") {
        e.preventDefault();
        setOpen(true);
      }
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, []);
  return (
    <>
      <button className="ds-btn" onClick={() => setOpen(true)}>
        Open Command Palette <Kbd size="sm">⌘K</Kbd>
      </button>
      <CommandPalette open={open} onClose={() => setOpen(false)} items={ITEMS} />
    </>
  );
}
```

Include a `SRC` map of source snippets for `parameters.docs.source.code`.

Set Meta: `title: "Overlays/CommandPalette"`, `tags: ["autodocs"]`, `layout: "centered"`.
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -c "^export const " src/overlays/CommandPalette/CommandPalette.stories.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exports at least 4 stories
    - DarkMode story has className="dark" decorator
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>4+ stories exported, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Barrel export + phase gate</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (find overlays section, locate ConfirmDialog block around line 55)
  </read_first>
  <action>
Insert the following export immediately after the ConfirmDialog block in `src/index.ts`:

```typescript
export {
  CommandPalette,
  type CommandPaletteProps,
  type CommandPaletteItem,
} from "./overlays/CommandPalette";
```

Then run:
1. `npx tsc --noEmit` — must exit 0
2. `npx vitest run` — Phase 22 tests pass; pre-existing failures (calendarGrid, Calendar, CopyToClipboard) unchanged
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -c "CommandPalette" src/index.ts && npx tsc --noEmit && npx vitest run src/overlays/CommandPalette 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/index.ts contains export for CommandPalette, CommandPaletteProps, CommandPaletteItem
    - tsc --noEmit exits 0
    - Phase 22 tests pass
  </acceptance_criteria>
  <done>Exports wired. Phase gate passes.</done>
</task>

</tasks>

<verification>
1. Stories file exports >= 4 names
2. `grep -c "CommandPalette" src/index.ts` returns at least 3
3. `npx tsc --noEmit` exits 0
4. `npx vitest run` — Phase 22 tests pass
</verification>

<success_criteria>
- 4+ stories shipped with Cmd+K demo wiring
- CommandPalette importable from package root
- tsc clean, tests pass
</success_criteria>

<output>
Create `.planning/phases/022-commandpalette/022-03-SUMMARY.md` after completion.
</output>
