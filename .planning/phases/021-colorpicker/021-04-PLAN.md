---
phase: 021-colorpicker
plan: 04
type: execute
wave: 3
depends_on:
  - "021-02"
  - "021-03"
files_modified:
  - src/inputs/ColorPicker/ColorPicker.stories.tsx
  - src/inputs/ColorPicker/ColorInput.stories.tsx
  - src/index.ts
autonomous: true
requirements:
  - REQ-21-01

must_haves:
  truths:
    - "ColorPicker.stories.tsx exports stories: Default, Controlled, CustomPresets, DarkMode"
    - "ColorInput.stories.tsx exports stories: Default, WithLabel, InFormRow, DarkMode"
    - "src/index.ts exports ColorPicker, ColorPickerProps, ColorInput, ColorInputProps"
    - "tsc --noEmit exits 0"
    - "Full vitest suite: ColorPicker tests + ColorInput tests + colorUtils tests all pass"
---

<objective>
Add Storybook stories for both ColorPicker and ColorInput, wire into barrel exports, and run final phase gate (tsc + full vitest).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/021-colorpicker/021-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ColorPicker stories</name>
  <files>src/inputs/ColorPicker/ColorPicker.stories.tsx</files>
  <read_first>
    - src/inputs/DatePicker/DatePicker.stories.tsx (stories pattern with controlled wrapper)
    - src/inputs/ColorPicker/index.tsx
  </read_first>
  <action>
Create `src/inputs/ColorPicker/ColorPicker.stories.tsx` with 4 stories: Default (uncontrolled playground), Controlled (with useState wrapper), CustomPresets (with passed presets array), DarkMode (className="dark" decorator on a #1c1917 background).

Follow the DatePicker.stories.tsx pattern with `Meta<typeof ColorPicker>`, `tags: ["autodocs"]`, `layout: "centered"`, controlled stories use a `function ColorPickerDemo()` wrapper with useState, render: () => <ColorPickerDemo />.

DarkMode decorator:
```tsx
decorators: [
  (Story) => (
    <div className="dark" style={{ background: "#1c1917", padding: 32 }}>
      <Story />
    </div>
  ),
],
```
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -c "^export const " src/inputs/ColorPicker/ColorPicker.stories.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exports at least 4 stories (Default, Controlled, CustomPresets, DarkMode)
    - DarkMode story has className="dark" decorator
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>4+ stories exported, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: ColorInput stories</name>
  <files>src/inputs/ColorPicker/ColorInput.stories.tsx</files>
  <read_first>
    - src/inputs/ColorPicker/ColorInput.tsx
    - src/inputs/TextInput/TextInput.stories.tsx (form-row pattern)
  </read_first>
  <action>
Create `src/inputs/ColorPicker/ColorInput.stories.tsx` with 4 stories: Default, WithLabel, InFormRow (showing ColorInput alongside other ds-input fields), DarkMode.
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -c "^export const " src/inputs/ColorPicker/ColorInput.stories.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exports at least 4 stories
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>4+ stories exported, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Barrel exports + phase gate</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (current exports — find inputs section)
  </read_first>
  <action>
Add the following exports to `src/index.ts` in the inputs section (near DatePicker / DateRangePicker):

```typescript
export { ColorPicker, type ColorPickerProps } from "./inputs/ColorPicker";
export { ColorInput, type ColorInputProps } from "./inputs/ColorPicker/ColorInput";
```

Note: src/inputs/ColorPicker/index.tsx exports ColorPicker. ColorInput is in a sibling file, so its export path is "./inputs/ColorPicker/ColorInput".

Then run:
1. `npx tsc --noEmit` — must exit 0
2. `npx vitest run` — Phase 21 tests must pass; pre-existing failures (calendarGrid, Calendar, CopyToClipboard) unchanged
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -c "ColorPicker\|ColorInput" src/index.ts && npx tsc --noEmit && npx vitest run src/inputs/ColorPicker 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/index.ts contains export for ColorPicker, ColorInput, ColorPickerProps, ColorInputProps
    - tsc --noEmit exits 0
    - All Phase 21 tests pass
  </acceptance_criteria>
  <done>Both components exported. Phase gate passes.</done>
</task>

</tasks>

<verification>
1. `grep -c "ColorPicker\|ColorInput" src/index.ts` returns at least 4
2. `npx tsc --noEmit` exits 0
3. Full vitest suite: Phase 21 tests pass; pre-existing 3 failures unchanged
</verification>

<success_criteria>
- All 8 stories shipped
- Both components exported from package root
- tsc clean, tests pass
</success_criteria>

<output>
Create `.planning/phases/021-colorpicker/021-04-SUMMARY.md` after completion.
</output>
