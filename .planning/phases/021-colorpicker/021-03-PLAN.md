---
phase: 021-colorpicker
plan: 03
type: execute
wave: 2
depends_on:
  - "021-01"
files_modified:
  - src/inputs/ColorPicker/ColorInput.tsx
  - src/inputs/ColorPicker/ColorInput.test.tsx
autonomous: true
requirements:
  - REQ-21-01

must_haves:
  truths:
    - "ColorInput renders a swatch + hex text field inside ds-input-wrap"
    - "Typing a valid hex updates the swatch background color"
    - "Partial hex input does NOT call onChange"
    - "Component fits in form rows alongside other ds-input fields"
    - "ColorInput does NOT open a popover or expand into the full picker (standalone only)"
  artifacts:
    - path: "src/inputs/ColorPicker/ColorInput.tsx"
      provides: "Inline ColorInput field"
      exports: ["ColorInput", "ColorInputProps"]
    - path: "src/inputs/ColorPicker/ColorInput.test.tsx"
      provides: "Unit tests for ColorInput"
      contains: 'describe("ColorInput"'
---

<objective>
Implement ColorInput — a compact inline form-field variant: 28x28 color swatch + hex text input, designed to fit inside a form row. NO picker popover (per RESEARCH.md decision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/021-colorpicker/021-RESEARCH.md
@.planning/phases/021-colorpicker/021-PATTERNS.md

<interfaces>
ColorInputProps:
```typescript
export interface ColorInputProps {
  value?: string;
  onChange?: (hex: string) => void;
  defaultValue?: string;
  label?: string;
  className?: string;
  style?: CSSProperties;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ColorInput inline variant</name>
  <files>
    - src/inputs/ColorPicker/ColorInput.tsx
    - src/inputs/ColorPicker/ColorInput.test.tsx
  </files>
  <read_first>
    - src/inputs/TextInput/index.tsx (ds-input-wrap pattern)
    - src/inputs/ColorPicker/colorUtils.ts
  </read_first>
  <behavior>
    - Renders a horizontal flex row with swatch (28x28 colored div) + ds-input
    - Typing a valid 6-digit hex updates color and calls onChange
    - Partial typing does not call onChange
    - Optional label prop renders above the row using ds-label class
    - Compact (no presets, no canvas, no bars)
  </behavior>
  <action>
Create `src/inputs/ColorPicker/ColorInput.tsx`:

```typescript
import { type CSSProperties, type ChangeEvent, useEffect, useState } from "react";

const HEX_RE = /^#[0-9a-fA-F]{6}$/;

export interface ColorInputProps {
  value?: string;
  onChange?: (hex: string) => void;
  defaultValue?: string;
  label?: string;
  className?: string;
  style?: CSSProperties;
}

export function ColorInput({
  value,
  onChange,
  defaultValue = '#f59e0b',
  label,
  className,
  style,
}: ColorInputProps) {
  const initial = value ?? defaultValue;
  const [color, setColor] = useState<string>(initial);
  const [hex, setHex] = useState<string>(initial);

  useEffect(() => {
    if (value !== undefined && value !== color) {
      setColor(value);
      setHex(value);
    }
  }, [value]); // eslint-disable-line react-hooks/exhaustive-deps

  function handleChange(e: ChangeEvent<HTMLInputElement>) {
    const next = e.target.value;
    setHex(next);
    if (HEX_RE.test(next)) {
      setColor(next);
      onChange?.(next);
    }
  }

  return (
    <div className={["ds-atom-colorinput", className].filter(Boolean).join(" ")}
         style={style}>
      {label && (
        <label className="ds-label" style={{ display: 'block', marginBottom: 4 }}>
          {label}
        </label>
      )}
      <div className="ds-input-wrap" style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
        <div
          aria-hidden="true"
          style={{
            width: 28, height: 28, borderRadius: 6, background: color,
            border: '1px solid var(--rule)', flexShrink: 0,
          }}
        />
        <input
          className="ds-input"
          value={hex}
          onChange={handleChange}
          aria-label={label ?? "Color hex"}
          style={{ fontFamily: 'var(--mono)', fontSize: 12, flex: 1 }}
        />
      </div>
    </div>
  );
}
```

Create `src/inputs/ColorPicker/ColorInput.test.tsx`:

```typescript
import { describe, it, expect, vi } from "vitest";
import { render, fireEvent } from "@testing-library/react";
import { ColorInput } from "./ColorInput";

describe("ColorInput", () => {
  it("renders swatch + input", () => {
    const { container } = render(<ColorInput />);
    expect(container.querySelector(".ds-atom-colorinput")).not.toBeNull();
    expect(container.querySelector(".ds-input-wrap")).not.toBeNull();
    expect(container.querySelector("input.ds-input")).not.toBeNull();
  });

  it("renders label when provided", () => {
    const { container } = render(<ColorInput label="Primary color" />);
    expect(container.querySelector("label.ds-label")?.textContent).toBe("Primary color");
  });

  it("typing valid hex calls onChange", () => {
    const onChange = vi.fn();
    const { container } = render(<ColorInput onChange={onChange} />);
    const input = container.querySelector<HTMLInputElement>("input.ds-input")!;
    fireEvent.change(input, { target: { value: "#abcdef" } });
    expect(onChange).toHaveBeenCalledWith("#abcdef");
  });

  it("typing partial hex does not call onChange", () => {
    const onChange = vi.fn();
    const { container } = render(<ColorInput onChange={onChange} />);
    const input = container.querySelector<HTMLInputElement>("input.ds-input")!;
    fireEvent.change(input, { target: { value: "#abc" } });
    expect(onChange).not.toHaveBeenCalled();
  });

  it("controlled value updates swatch", () => {
    const { container, rerender } = render(<ColorInput value="#ff0000" />);
    rerender(<ColorInput value="#3b82f6" />);
    const swatch = container.querySelector<HTMLDivElement>(".ds-input-wrap > div")!;
    expect(swatch.style.background).toContain("rgb(59, 130, 246)");
  });

  it("uses defaultValue in uncontrolled mode", () => {
    const { container } = render(<ColorInput defaultValue="#22c55e" />);
    const input = container.querySelector<HTMLInputElement>("input.ds-input")!;
    expect(input.value).toBe("#22c55e");
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && npx vitest run src/inputs/ColorPicker/ColorInput.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/inputs/ColorPicker/ColorInput.tsx exists, exports ColorInput + ColorInputProps
    - All 6 tests pass
    - grep -c "ds-input-wrap" src/inputs/ColorPicker/ColorInput.tsx returns 1
  </acceptance_criteria>
  <done>ColorInput component shipped. 6 tests pass. tsc clean.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/inputs/ColorPicker/ColorInput.test.tsx` — passes
2. `npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- ColorInput is a compact inline field
- Hex validation guards onChange
- Fits inside ds-input-wrap form rows
- 6 tests pass
</success_criteria>

<output>
Create `.planning/phases/021-colorpicker/021-03-SUMMARY.md` after completion.
</output>
