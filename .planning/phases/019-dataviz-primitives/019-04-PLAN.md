---
phase: 019-dataviz-primitives
plan: "04"
type: execute
wave: 2
depends_on:
  - "019-01"
  - "019-02"
  - "019-03"
files_modified:
  - src/index.ts
autonomous: true
requirements:
  - REQ-19-01
  - REQ-19-02
  - REQ-19-03
must_haves:
  truths:
    - "Sparkline, SparklineProps exported from src/index.ts"
    - "MiniDonut, MiniDonutProps exported from src/index.ts"
    - "MiniBar, MiniBarProps exported from src/index.ts"
    - "tsc --noEmit exits 0 with no errors"
    - "All 18 Phase 19 tests pass (7 Sparkline + 6 MiniDonut + 5 MiniBar)"
  artifacts:
    - path: "src/index.ts"
      provides: "Barrel exports for all three dataviz primitives"
      contains: "from \"./display/Sparkline\""
  key_links:
    - from: "src/index.ts"
      to: "src/display/Sparkline/index.tsx"
      via: "named re-export"
      pattern: "from \"./display/Sparkline\""
    - from: "src/index.ts"
      to: "src/display/MiniDonut/index.tsx"
      via: "named re-export"
      pattern: "from \"./display/MiniDonut\""
    - from: "src/index.ts"
      to: "src/display/MiniBar/index.tsx"
      via: "named re-export"
      pattern: "from \"./display/MiniBar\""
---

<objective>
Wire all three Phase 19 components into the public barrel export and verify the full phase: TypeScript clean compile, all 18 unit tests green.

Purpose: Consumers of @akhil-saxena/design-system must be able to import Sparkline, MiniDonut, and MiniBar from the package root without reaching into internal paths.
Output: Three new export lines in src/index.ts; verified tsc + full test suite.
</objective>

<execution_context>
@/Users/temp/Documents/workspace/design-system/.claude/get-shit-done/workflows/execute-plan.md
@/Users/temp/Documents/workspace/design-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/temp/Documents/workspace/design-system/.planning/ROADMAP.md
@/Users/temp/Documents/workspace/design-system/.planning/STATE.md
@/Users/temp/Documents/workspace/design-system/.planning/phases/019-dataviz-primitives/019-01-SUMMARY.md
@/Users/temp/Documents/workspace/design-system/.planning/phases/019-dataviz-primitives/019-02-SUMMARY.md
@/Users/temp/Documents/workspace/design-system/.planning/phases/019-dataviz-primitives/019-03-SUMMARY.md

<interfaces>
<!-- Exact export pattern to follow from src/index.ts line 24 -->
Existing display primitive export (single-type pattern, line 24):
```typescript
export { RollingNumber, type RollingNumberProps } from "./display/RollingNumber";
```

Insertion point: After line 24 (after the RollingNumber export line), before line 25 (RangeSlider).
The display section in src/index.ts currently ends at line 24. The three new lines go immediately after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add barrel exports to src/index.ts</name>
  <files>src/index.ts</files>
  <action>
    Read src/index.ts to locate line 24:
      export { RollingNumber, type RollingNumberProps } from "./display/RollingNumber";

    Insert three lines immediately after line 24 (before the RangeSlider export on line 25):

    ```typescript
    export { Sparkline, type SparklineProps } from "./display/Sparkline";
    export { MiniDonut, type MiniDonutProps } from "./display/MiniDonut";
    export { MiniBar, type MiniBarProps } from "./display/MiniBar";
    ```

    These lines follow the identical single-type export pattern used by RollingNumber.
    Do not append to end of file — insert in the display section alongside the other display primitives.

    After editing, verify the three lines exist in the file:
      grep -c 'from "./display/Sparkline"' src/index.ts  → must equal 1
      grep -c 'from "./display/MiniDonut"' src/index.ts  → must equal 1
      grep -c 'from "./display/MiniBar"' src/index.ts    → must equal 1
  </action>
  <verify>
    <automated>grep -v '^//' /Users/temp/Documents/workspace/design-system/src/index.ts | grep -c 'from "./display/Sparkline"'</automated>
  </verify>
  <done>src/index.ts contains export lines for Sparkline, MiniDonut, and MiniBar in the display section</done>
</task>

<task type="auto">
  <name>Task 2: Full phase verification — tsc + test suite</name>
  <files></files>
  <action>
    Run TypeScript compiler and full test suite:

    Step 1 — TypeScript clean compile:
      cd /Users/temp/Documents/workspace/design-system && npx tsc --noEmit
    Must exit 0 with no output. If errors appear, read them and fix — do NOT proceed to tests until tsc is clean.

    Step 2 — Full display test suite:
      npx vitest run src/display/Sparkline/Sparkline.test.tsx src/display/MiniDonut/MiniDonut.test.tsx src/display/MiniBar/MiniBar.test.tsx
    Must show 18 tests passing (7 + 6 + 5).

    Step 3 — Full project test suite:
      npx vitest run
    Must exit 0. If any pre-existing tests fail, report them — do NOT fix unrelated failures as part of this plan.

    If tsc or tests fail due to issues introduced by this plan (import paths, type mismatches), fix them in src/index.ts or the component files before declaring done.
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && npx tsc --noEmit && npx vitest run src/display/Sparkline/Sparkline.test.tsx src/display/MiniDonut/MiniDonut.test.tsx src/display/MiniBar/MiniBar.test.tsx</automated>
  </verify>
  <done>tsc exits 0; 18 Phase 19 tests pass; full vitest run exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package barrel→consumer | Public API surface exposed via src/index.ts re-exports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-019-04-01 | Information Disclosure | Exporting internal types unintentionally | accept | Only Props interfaces are exported alongside components; no internal helper types are exposed |
</threat_model>

<verification>
npx tsc --noEmit
npx vitest run src/display/Sparkline src/display/MiniDonut src/display/MiniBar
</verification>

<success_criteria>
- src/index.ts exports Sparkline, SparklineProps, MiniDonut, MiniDonutProps, MiniBar, MiniBarProps
- tsc --noEmit exits 0
- 18 unit tests pass (7 Sparkline + 6 MiniDonut + 5 MiniBar)
- Full vitest run exits 0
</success_criteria>

<output>
After completion, create .planning/phases/019-dataviz-primitives/019-04-SUMMARY.md
</output>
