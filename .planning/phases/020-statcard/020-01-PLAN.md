---
phase: 020-statcard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/display/StatCard/index.tsx
  - src/display/StatCard/StatCard.test.tsx
autonomous: true
requirements:
  - REQ-20-01

must_haves:
  truths:
    - "StatCard renders a mono uppercase label in --ink-3 at 9px with 0.08em letter-spacing"
    - "StatCard renders a value using var(--display) Archivo at 800 weight 28px with -0.02em letter-spacing"
    - "When changeDir='up' the badge background is rgba(34,197,94,.1) and text color is var(--green)"
    - "When changeDir='down' the badge background is rgba(239,68,68,.08) and text color is var(--red)"
    - "When change prop is absent no badge element is rendered"
    - "When data prop is present and length >= 2 a Sparkline is rendered inside a full-width wrapper"
    - "When data prop is absent or has < 2 points no Sparkline is rendered"
    - "Root element carries className='glass' and inline style padding:16, borderRadius:12"
  artifacts:
    - path: "src/display/StatCard/index.tsx"
      provides: "StatCard component and StatCardProps type export"
      exports: ["StatCard", "StatCardProps", "StatCardChangeDir"]
    - path: "src/display/StatCard/StatCard.test.tsx"
      provides: "Unit tests covering all 8 behavioral contracts above"
      contains: "describe(\"StatCard\""
  key_links:
    - from: "src/display/StatCard/index.tsx"
      to: "src/display/Sparkline/index.tsx"
      via: "import { Sparkline } from '../Sparkline'"
      pattern: "from.*Sparkline"
    - from: "src/display/StatCard/index.tsx"
      to: ".glass CSS class"
      via: "className=\"glass\""
      pattern: "className.*glass"
---

<objective>
Implement the StatCard component and its unit-test suite.

Purpose: Phase 20 delivers a single-component KPI card that composes the glass surface, mono label, Archivo value display, trend badge pill, and optional Sparkline into a single drop-in dashboard unit. This plan creates the component and the test contracts that gate its correctness.

Output:
- `src/display/StatCard/index.tsx` — component + exported types
- `src/display/StatCard/StatCard.test.tsx` — 8 unit tests covering every behavioral branch
</objective>

<execution_context>
@/Users/temp/Documents/workspace/design-system/.claude/get-shit-done/workflows/execute-plan.md
@/Users/temp/Documents/workspace/design-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/temp/Documents/workspace/design-system/.planning/PROJECT.md
@/Users/temp/Documents/workspace/design-system/.planning/ROADMAP.md
@/Users/temp/Documents/workspace/design-system/.planning/REQUIREMENTS.md
@/Users/temp/Documents/workspace/design-system/.planning/phases/020-statcard/020-RESEARCH.md
@/Users/temp/Documents/workspace/design-system/.planning/phases/020-statcard/020-PATTERNS.md

<interfaces>
<!-- Phase 19 Sparkline component — executor must verify this file exists at
     src/display/Sparkline/index.tsx before running. If it does not exist,
     Phase 19 has not shipped yet — stop and surface the blocker. -->

Expected Sparkline export from src/display/Sparkline/index.tsx:
```typescript
export interface SparklineProps {
  data: number[];          // required — min 2 points for a valid polyline
  width?: number;          // default 100
  height?: number;         // default 28
  color?: string;          // default 'var(--amber)'
  fill?: boolean;          // default true — renders 10% opacity fill path
}
export function Sparkline(props: SparklineProps): JSX.Element;
```

<!-- Closest analog for component structure — RollingNumber (named export, no forwardRef,
     plain props, no side-effects): -->
From src/display/RollingNumber/index.tsx:
```typescript
export interface RollingNumberProps {
  value: number;
  className?: string;
  style?: CSSProperties;
}
export function RollingNumber({ value, className, style }: RollingNumberProps) {
  return (
    <span
      className={["ds-atom-rolling", className].filter(Boolean).join(" ")}
      style={style}
    >
      ...
    </span>
  );
}
```

<!-- Glass class from src/utilities.css (VERIFIED):
  .glass {
    background: var(--g-bg);
    backdrop-filter: blur(var(--g-blur));
    -webkit-backdrop-filter: blur(var(--g-blur));
    border: 1px solid var(--g-bd);
    border-radius: var(--radius-xl);   ← 16px by default
  }
  StatCard overrides border-radius to 12 via inline style (inline wins over class rule).
-->

<!-- Test analog structure from src/display/RollingNumber/RollingNumber.test.tsx: -->
```typescript
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import { RollingNumber } from ".";

describe("RollingNumber", () => {
  it("renders the numeric value as text", () => {
    render(<RollingNumber value={42} />);
    expect(screen.getByText("42")).toBeInTheDocument();
  });
  it("does not render suffix when omitted", () => {
    const { container } = render(<RollingNumber value={42} />);
    expect(container.querySelector(".ds-atom-rolling-static")).toBeNull();
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write StatCard test suite (RED)</name>
  <files>src/display/StatCard/StatCard.test.tsx</files>
  <behavior>
    - Test 1: Renders label text — `render(<StatCard label="Applications" value="24" />)` → `screen.getByText("Applications")` is in document
    - Test 2: Label element has correct inline styles — container.querySelector on the label div has style containing `fontFamily` matching `var(--mono)`, `fontSize` "9px", `letterSpacing` ".08em", `textTransform` "uppercase", `fontWeight` 700, `color` "var(--ink-3)"
    - Test 3: Value text renders — `screen.getByText("24")` is in document
    - Test 4: Positive badge renders green — `render(<StatCard label="L" value="1" change="+12%" changeDir="up" />)` → badge div has `background: rgba(34,197,94,.1)` and `color: var(--green)` in its inline style
    - Test 5: Negative badge renders red — `changeDir="down"` → badge div has `background: rgba(239,68,68,.08)` and `color: var(--red)` in inline style
    - Test 6: No badge when change prop absent — no badge div in container when change is omitted
    - Test 7: Sparkline wrapper renders when data.length >= 2 — `data={[1,2,3]}` → container contains a Sparkline SVG element (presence of `<svg>`)
    - Test 8: No Sparkline when data absent — container has no `<svg>` when data is not provided
    - Test 9: Root element has className containing "glass" — `container.firstElementChild.className` includes "glass"
    - Test 10: Root element has inline style borderRadius 12 — `container.firstElementChild.style.borderRadius` equals "12px"

    NOTE: Tests will fail (RED) until Task 2 creates the component. Run `npm test -- src/display/StatCard/StatCard.test.tsx` and confirm failures before proceeding to Task 2.

    Mocking Sparkline: Since Sparkline depends on Phase 19 which may not exist at test time, add a vi.mock at the top of the test file:
    ```typescript
    vi.mock("../Sparkline", () => ({
      Sparkline: ({ data }: { data: number[] }) => <svg data-testid="sparkline" data-points={data.length} />,
    }));
    ```
    This lets tests run regardless of Phase 19 completion status.
  </behavior>
  <action>
    Create `src/display/StatCard/StatCard.test.tsx` with the following exact structure:

    ```typescript
    import { render, screen } from "@testing-library/react";
    import { describe, expect, it, vi } from "vitest";
    import { StatCard } from ".";

    vi.mock("../Sparkline", () => ({
      Sparkline: ({ data }: { data: number[] }) => (
        <svg data-testid="sparkline" data-points={data.length} />
      ),
    }));

    describe("StatCard", () => {
      it("renders the label text", () => {
        render(<StatCard label="Applications" value="24" />);
        expect(screen.getByText("Applications")).toBeInTheDocument();
      });

      it("label element has correct inline styles", () => {
        const { container } = render(<StatCard label="Applications" value="24" />);
        const label = container.querySelector<HTMLElement>("[data-part='label']");
        expect(label).not.toBeNull();
        expect(label!.style.fontFamily).toBe("var(--mono)");
        expect(label!.style.fontSize).toBe("9px");
        expect(label!.style.letterSpacing).toBe(".08em");
        expect(label!.style.textTransform).toBe("uppercase");
        expect(label!.style.color).toBe("var(--ink-3)");
      });

      it("renders the value text", () => {
        render(<StatCard label="L" value="24" />);
        expect(screen.getByText("24")).toBeInTheDocument();
      });

      it("positive changeDir renders green badge", () => {
        const { container } = render(
          <StatCard label="L" value="1" change="+12%" changeDir="up" />
        );
        const badge = container.querySelector<HTMLElement>("[data-part='badge']");
        expect(badge).not.toBeNull();
        expect(badge!.style.background).toBe("rgba(34,197,94,.1)");
        expect(badge!.style.color).toBe("var(--green)");
      });

      it("negative changeDir renders red badge", () => {
        const { container } = render(
          <StatCard label="L" value="1" change="-5%" changeDir="down" />
        );
        const badge = container.querySelector<HTMLElement>("[data-part='badge']");
        expect(badge).not.toBeNull();
        expect(badge!.style.background).toBe("rgba(239,68,68,.08)");
        expect(badge!.style.color).toBe("var(--red)");
      });

      it("does not render badge when change prop is absent", () => {
        const { container } = render(<StatCard label="L" value="1" />);
        expect(container.querySelector("[data-part='badge']")).toBeNull();
      });

      it("renders Sparkline when data has 2 or more points", () => {
        const { container } = render(
          <StatCard label="L" value="1" data={[1, 2, 3]} />
        );
        expect(container.querySelector("svg")).not.toBeNull();
      });

      it("does not render Sparkline when data is absent", () => {
        const { container } = render(<StatCard label="L" value="1" />);
        expect(container.querySelector("svg")).toBeNull();
      });

      it("does not render Sparkline when data has fewer than 2 points", () => {
        const { container } = render(<StatCard label="L" value="1" data={[5]} />);
        expect(container.querySelector("svg")).toBeNull();
      });

      it("root element carries the glass class", () => {
        const { container } = render(<StatCard label="L" value="1" />);
        expect(container.firstElementChild?.className).toContain("glass");
      });

      it("root element has borderRadius 12 inline style", () => {
        const { container } = render(<StatCard label="L" value="1" />);
        const root = container.firstElementChild as HTMLElement;
        expect(root.style.borderRadius).toBe("12px");
      });
    });
    ```

    After writing: run `npm test -- src/display/StatCard/StatCard.test.tsx` and confirm all tests FAIL with import errors (the component does not exist yet). This is the expected RED state.
  </action>
  <verify>
    <automated>npm test -- src/display/StatCard/StatCard.test.tsx 2>&1 | grep -E "FAIL|Cannot find module|ERR"</automated>
  </verify>
  <done>StatCard.test.tsx exists and all tests fail with "Cannot find module" or import errors — confirming RED state before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement StatCard component (GREEN)</name>
  <files>src/display/StatCard/index.tsx</files>
  <behavior>
    Component must satisfy all 11 test expectations from Task 1. Key behavioral contracts:
    - Named function export `StatCard` (not default, not forwardRef — matches RollingNumber pattern per planning decision)
    - Exported interface `StatCardProps` with: label, value, change?, changeDir?: "up"|"down", data?: number[], className?, style?
    - Exported type `StatCardChangeDir = "up" | "down"`
    - Root div: `className={["glass", className].filter(Boolean).join(" ")}` + `style={{ padding: 16, borderRadius: 12, ...style }}`
    - Label div: `data-part="label"` attribute (for test selector), inline styles exactly: fontFamily "var(--mono)", fontSize 9, color "var(--ink-3)", letterSpacing ".08em", textTransform "uppercase", fontWeight 700
    - Value+badge row: `display: "flex"`, `justifyContent: "space-between"`, `alignItems: "flex-start"`
    - Value div: fontFamily "var(--display)", fontWeight 800, fontSize 28, letterSpacing "-.02em", marginTop 4
    - Badge: rendered only when `change` prop is truthy; `data-part="badge"` attribute; inline styles: padding "3px 7px", borderRadius 4, fontFamily "var(--mono)", fontSize 10, fontWeight 700; background rgba(34,197,94,.1) when `up === true`, rgba(239,68,68,.08) when `up === false`; color var(--green) when up, var(--red) when down
    - sentimentColor: changeDir === "up" ? "var(--green)" : changeDir === "down" ? "var(--red)" : "var(--amber)"
    - Sparkline wrapper: rendered only when `data && data.length >= 2`; wrapper div has `marginTop: 10`; Sparkline receives `data={data}` and `color={sentimentColor}`; Sparkline width is not overridden (defaults to 100 per Phase 19 spec — the wrapper provides visual context)
    - Import Sparkline: `import { Sparkline } from "../Sparkline";`
  </behavior>
  <action>
    FIRST: Verify Phase 19 Sparkline exists:
    ```bash
    ls src/display/Sparkline/index.tsx
    ```
    If the file does NOT exist, Phase 19 has not shipped. Do NOT proceed — surface the blocker:
    "BLOCKER: src/display/Sparkline/index.tsx does not exist. Phase 19 must be complete before Phase 20 can be executed."

    If Phase 19 IS complete, create `src/display/StatCard/index.tsx`:

    ```typescript
    import type { CSSProperties } from "react";
    import { Sparkline } from "../Sparkline";

    export type StatCardChangeDir = "up" | "down";

    export interface StatCardProps {
      /** Monospace uppercase label, e.g. "Total Applications" */
      label: string;
      /** Primary metric value as string or number, e.g. "24", "42%", "4.2d" */
      value: string | number;
      /** Change string displayed in trend badge, e.g. "+12%", "-5%" */
      change?: string;
      /** Trend direction — determines badge and sparkline color */
      changeDir?: StatCardChangeDir;
      /** Sparkline data points — renders chart when provided (min 2 points) */
      data?: number[];
      /** Additional className forwarded to the root div */
      className?: string;
      /** Additional inline styles for the root div */
      style?: CSSProperties;
    }

    export function StatCard({
      label,
      value,
      change,
      changeDir,
      data,
      className,
      style,
    }: StatCardProps) {
      const up = changeDir === "up";
      const sentimentColor =
        changeDir === "up"
          ? "var(--green)"
          : changeDir === "down"
            ? "var(--red)"
            : "var(--amber)";

      return (
        <div
          className={["glass", className].filter(Boolean).join(" ")}
          style={{ padding: 16, borderRadius: 12, ...style }}
        >
          {/* Label */}
          <div
            data-part="label"
            style={{
              fontFamily: "var(--mono)",
              fontSize: 9,
              color: "var(--ink-3)",
              letterSpacing: ".08em",
              textTransform: "uppercase",
              fontWeight: 700,
            }}
          >
            {label}
          </div>

          {/* Value + badge row */}
          <div
            style={{
              display: "flex",
              justifyContent: "space-between",
              alignItems: "flex-start",
            }}
          >
            <div
              style={{
                fontFamily: "var(--display)",
                fontWeight: 800,
                fontSize: 28,
                letterSpacing: "-.02em",
                marginTop: 4,
              }}
            >
              {value}
            </div>

            {change && (
              <div
                data-part="badge"
                style={{
                  padding: "3px 7px",
                  borderRadius: 4,
                  background: up
                    ? "rgba(34,197,94,.1)"
                    : "rgba(239,68,68,.08)",
                  fontFamily: "var(--mono)",
                  fontSize: 10,
                  fontWeight: 700,
                  color: up ? "var(--green)" : "var(--red)",
                }}
              >
                {change}
              </div>
            )}
          </div>

          {/* Sparkline */}
          {data && data.length >= 2 && (
            <div style={{ marginTop: 10 }}>
              <Sparkline data={data} color={sentimentColor} />
            </div>
          )}
        </div>
      );
    }
    ```

    After writing: run the test suite to confirm GREEN.
  </action>
  <verify>
    <automated>npm test -- src/display/StatCard/StatCard.test.tsx</automated>
  </verify>
  <done>All 11 tests in StatCard.test.tsx pass. npm test exits 0. No TypeScript errors in the file (tsc --noEmit confirms clean).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer props → component | label, value, change strings come from caller; rendered into DOM via React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-020-01 | Spoofing | StatCard label/value | accept | Pure display component — no auth context; strings rendered as React text (auto-escaped), no dangerouslySetInnerHTML |
| T-020-02 | Information Disclosure | StatCard | accept | No PII handling; KPI values are dashboard metrics; component has no network calls |
| T-020-03 | Denial of Service | data prop | accept | data is number[] — React handles large arrays gracefully; no infinite loop risk; Sparkline owns normalization |
</threat_model>

<verification>
```bash
# 1. All StatCard unit tests pass
npm test -- src/display/StatCard/StatCard.test.tsx

# 2. TypeScript clean
npx tsc --noEmit

# 3. Component file exists with correct exports
grep -v '^//' src/display/StatCard/index.tsx | grep -c "export"
# Expected: >= 3 (StatCard, StatCardProps, StatCardChangeDir)
```
</verification>

<success_criteria>
- `src/display/StatCard/index.tsx` exists and exports `StatCard`, `StatCardProps`, `StatCardChangeDir`
- All 11 unit tests in `StatCard.test.tsx` pass
- `tsc --noEmit` exits 0
- No badge is rendered when `change` prop is absent
- No Sparkline is rendered when `data` is absent or `data.length < 2`
- Root element carries `glass` class and `borderRadius: 12px` inline style
</success_criteria>

<output>
After completion, create `.planning/phases/020-statcard/020-01-SUMMARY.md` following the summary template.
</output>
