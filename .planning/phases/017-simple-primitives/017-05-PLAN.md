---
phase: 017-simple-primitives
plan: 05
type: execute
wave: 3
depends_on:
  - "017-02"
  - "017-03"
  - "017-04"
files_modified:
  - src/index.ts
autonomous: true
requirements:
  - REQ-17-01
  - REQ-17-02
  - REQ-17-03

must_haves:
  truths:
    - "Kbd and KbdProps are exported from src/index.ts"
    - "RelativeTime and RelativeTimeProps are exported from src/index.ts"
    - "Pagination and PaginationProps are exported from src/index.ts"
    - "All three new exports are grouped correctly with their category peers"
    - "Full test suite passes (npm test) with no regressions"
    - "TypeScript build passes (tsc --noEmit) with no errors"
  artifacts:
    - path: "src/index.ts"
      provides: "Barrel exports for all three Phase 17 components"
      contains: "Kbd"
  key_links:
    - from: "src/index.ts"
      to: "src/inputs/Kbd/index.tsx"
      via: "export { Kbd, type KbdProps }"
      pattern: "from.*inputs/Kbd"
    - from: "src/index.ts"
      to: "src/interaction/RelativeTime/index.tsx"
      via: "export { RelativeTime, type RelativeTimeProps }"
      pattern: "from.*interaction/RelativeTime"
    - from: "src/index.ts"
      to: "src/data-display/Pagination/index.tsx"
      via: "export { Pagination, type PaginationProps }"
      pattern: "from.*data-display/Pagination"
---

<objective>
Add the three Phase 17 barrel exports to src/index.ts and run the full verification suite.

Purpose: Without barrel exports, consuming projects cannot import Kbd, RelativeTime, or Pagination from the design system package. This is the final gate before Phase 17 is complete.
Output: src/index.ts updated with 3 new export lines; full test suite green; TypeScript build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/017-simple-primitives/017-02-SUMMARY.md
@.planning/phases/017-simple-primitives/017-03-SUMMARY.md
@.planning/phases/017-simple-primitives/017-04-SUMMARY.md
</context>

<interfaces>
<!-- Existing export lines in src/index.ts that determine insertion positions -->

Line 6:  export { Badge, type BadgeProps, type BadgeTone } from "./inputs/Badge";
Line 70: export { CopyToClipboard, type CopyToClipboardProps } from "./interaction/CopyToClipboard";
Line 105: export { Tabs, type TabsProps, type TabItem } from "./data-display/Tabs";
Line 107: export { Calendar, type CalendarProps, type CalendarEvent } from "./data-display/Calendar";
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add barrel exports for Kbd, RelativeTime, and Pagination to src/index.ts</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (read full file — confirm current line count and exact text around lines 6, 70, 105–107 before editing)
    - src/inputs/Kbd/index.tsx (confirm exact named exports: Kbd, KbdProps, KbdSize)
    - src/interaction/RelativeTime/index.tsx (confirm exact named exports: RelativeTime, RelativeTimeProps)
    - src/data-display/Pagination/index.tsx (confirm exact named exports: Pagination, PaginationProps)
  </read_first>
  <action>
Read src/index.ts in full to identify exact current line numbers, then make three targeted insertions using the Edit tool. Do NOT rewrite the entire file.

**Insertion 1 — Kbd** (after the Badge export line):
Find the exact line: `export { Badge, type BadgeProps, type BadgeTone } from "./inputs/Badge";`
Insert immediately after it:
```typescript
export { Kbd, type KbdProps, type KbdSize } from "./inputs/Kbd";
```

**Insertion 2 — RelativeTime** (after the CopyToClipboard export line):
Find the exact line: `export { CopyToClipboard, type CopyToClipboardProps } from "./interaction/CopyToClipboard";`
Insert immediately after it:
```typescript
export { RelativeTime, type RelativeTimeProps } from "./interaction/RelativeTime";
```

**Insertion 3 — Pagination** (after the Calendar export line):
Find the exact line: `export { Calendar, type CalendarProps, type CalendarEvent } from "./data-display/Calendar";`
Insert immediately after it:
```typescript
export { Pagination, type PaginationProps } from "./data-display/Pagination";
```

Use three separate Edit operations (one per insertion) to minimize risk of conflicts. After all three edits, verify the file contains all three new lines using grep.
  </action>
  <verify>
    <automated>grep -c "Kbd\|RelativeTime\|Pagination" /Users/temp/Documents/workspace/design-system/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from.*inputs/Kbd" src/index.ts` returns exactly 1 match
    - `grep "from.*interaction/RelativeTime" src/index.ts` returns exactly 1 match
    - `grep "from.*data-display/Pagination" src/index.ts` returns exactly 1 match
    - `grep "type KbdProps\|type KbdSize" src/index.ts` returns matches (type exports present)
    - `grep "type RelativeTimeProps" src/index.ts` returns a match
    - `grep "type PaginationProps" src/index.ts` returns a match
    - File has no duplicate export lines (grep -c "from.*inputs/Kbd" returns exactly 1)
  </acceptance_criteria>
  <done>All 3 new export lines present in src/index.ts; no duplicate exports; file structure intact</done>
</task>

<task type="auto">
  <name>Task 2: Run full verification suite — tests + TypeScript build</name>
  <files></files>
  <read_first>
    - src/index.ts (confirm all 3 new exports are present before running suite)
  </read_first>
  <action>
Run the following verification commands in sequence. Report any failures.

Step 1 — TypeScript type check:
```bash
npx tsc --noEmit 2>&1
```
Expect: no output (clean exit). If errors appear, they are likely in the new component files — fix them before proceeding.

Step 2 — Phase 17 unit tests only:
```bash
npx vitest run src/inputs/Kbd src/interaction/RelativeTime src/data-display/Pagination --reporter=verbose 2>&1
```
Expect: all tests pass (green). Total test count should be >= 33 (8 Kbd + 11 RelativeTime + 14 Pagination).

Step 3 — Full test suite:
```bash
npm test 2>&1
```
Expect: all tests pass with zero failures. No regressions in previously passing tests.

If any step fails, diagnose and fix the root cause in the relevant component file before marking this task done.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
    - Phase 17 vitest run: all 33+ tests pass
    - `npm test` exits 0 (full suite green, no regressions)
    - Output does not contain "FAIL" or "error TS"
  </acceptance_criteria>
  <done>TypeScript build clean; all Phase 17 tests pass; full suite green with no regressions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Barrel exports | Re-exports only; no logic; no user data processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-017-06 | Tampering | src/index.ts | accept | Static re-export file; version-controlled; no runtime execution |
</threat_model>

<verification>
Final phase gate checks:

```bash
# 1. All three exports present
grep "from.*inputs/Kbd\|from.*interaction/RelativeTime\|from.*data-display/Pagination" src/index.ts

# 2. TypeScript clean
npx tsc --noEmit && echo "TypeScript OK"

# 3. Phase 17 tests
npx vitest run src/inputs/Kbd src/interaction/RelativeTime src/data-display/Pagination

# 4. Full suite
npm test
```

All four commands must pass before Phase 17 is considered complete.
</verification>

<success_criteria>
- src/index.ts exports Kbd + KbdProps + KbdSize from ./inputs/Kbd
- src/index.ts exports RelativeTime + RelativeTimeProps from ./interaction/RelativeTime
- src/index.ts exports Pagination + PaginationProps from ./data-display/Pagination
- TypeScript build exits 0
- Full test suite exits 0 with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/017-simple-primitives/017-05-SUMMARY.md`
</output>
