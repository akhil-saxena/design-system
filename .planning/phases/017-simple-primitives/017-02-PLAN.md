---
phase: 017-simple-primitives
plan: 02
type: execute
wave: 2
depends_on:
  - "017-01"
files_modified:
  - src/inputs/Kbd/index.tsx
  - src/inputs/Kbd/Kbd.stories.tsx
  - src/inputs/Kbd/Kbd.test.tsx
autonomous: true
requirements:
  - REQ-17-01

must_haves:
  truths:
    - "Kbd renders a <kbd> element (not <span> or <div>)"
    - "Kbd carries className ds-atom-kbd on the root element"
    - "Kbd size='sm' applies fontSize 9.5 and padding '1px 5px'"
    - "Kbd size='md' (default) applies fontSize 11 and padding '2px 6px'"
    - "Kbd forwards ref and spreads rest HTML attributes"
    - "Storybook shows Default, ModifierKeys, Combinations, SmSize, InlineInText, DarkMode stories"
    - "Unit tests pass covering element, class, size variants, ref, and rest spread"
  artifacts:
    - path: "src/inputs/Kbd/index.tsx"
      provides: "Kbd component"
      exports: ["Kbd", "KbdProps", "KbdSize"]
    - path: "src/inputs/Kbd/Kbd.stories.tsx"
      provides: "Storybook stories for Kbd"
      contains: "DarkMode"
    - path: "src/inputs/Kbd/Kbd.test.tsx"
      provides: "Unit tests for Kbd"
      contains: "ds-atom-kbd"
  key_links:
    - from: "src/inputs/Kbd/index.tsx"
      to: "src/primitives.css"
      via: "className='ds-atom-kbd'"
      pattern: "ds-atom-kbd"
---

<objective>
Implement the Kbd component, its Storybook stories, and its unit tests.

Purpose: Provides the semantic <kbd> keyboard-label primitive used in CommandPalette (Phase 22), ConfirmDialog (Phase 18), and inline docs.
Output: src/inputs/Kbd/ directory with index.tsx, Kbd.stories.tsx, Kbd.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/017-simple-primitives/017-UI-SPEC.md
@.planning/phases/017-simple-primitives/017-RESEARCH.md
@.planning/phases/017-simple-primitives/017-01-SUMMARY.md
</context>

<interfaces>
<!-- Analog: src/inputs/Badge/index.tsx — exact forwardRef + inline style pattern to follow -->

From src/inputs/Badge/index.tsx (structural reference):
```typescript
import { type CSSProperties, type HTMLAttributes, forwardRef } from "react";

export interface BadgeProps extends HTMLAttributes<HTMLSpanElement> {
  tone?: BadgeTone;
}

export const Badge = forwardRef<HTMLSpanElement, BadgeProps>(function Badge(
  { tone = "neutral", children, style, ...rest },
  ref,
) {
  return (
    <span ref={ref} style={{ ...baseStyle, ...toneStyles[tone], ...style }} {...rest}>
      {children}
    </span>
  );
});
```

From src/inputs/Badge/Badge.stories.tsx (DarkMode decorator shape):
```typescript
export const DarkMode: Story = {
  decorators: [
    (Story) => (
      <div
        className="dark"
        style={{ background: "#1c1917", padding: 16, borderRadius: 8, overflowX: "auto", minWidth: 0 }}
      >
        <Story />
      </div>
    ),
  ],
};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Kbd component (src/inputs/Kbd/index.tsx) with unit tests</name>
  <files>src/inputs/Kbd/index.tsx, src/inputs/Kbd/Kbd.test.tsx</files>
  <read_first>
    - src/inputs/Badge/index.tsx (read full file — inline style map + forwardRef pattern)
    - src/data-display/Breadcrumbs/Breadcrumbs.test.tsx (read first 60 lines — test import pattern)
  </read_first>
  <behavior>
    - Test 1: renders a `<kbd>` element (querySelector('kbd') is not null)
    - Test 2: root element has className containing 'ds-atom-kbd'
    - Test 3: renders children text content ("⌘K" appears in document)
    - Test 4: size="md" (default) — computed style has fontSize matching 11 (inline style attribute contains "11")
    - Test 5: size="sm" — inline style attribute contains "9.5"
    - Test 6: forwards ref — ref.current is the kbd DOM element
    - Test 7: spreads rest props — data-testid is present on rendered element
    - Test 8: custom className is appended — className contains both ds-atom-kbd and the custom class
  </behavior>
  <action>
Create src/inputs/Kbd/ directory. Write test file first (RED phase), then implement component (GREEN phase).

**Kbd.test.tsx** — imports: `{ render, screen }` from `@testing-library/react`, `{ describe, expect, it }` from `vitest`, `{ Kbd }` from `"."`. Use `createRef` from react for ref test. No axe import needed.

**index.tsx** — implement exactly as follows:

```typescript
import { type CSSProperties, type HTMLAttributes, forwardRef } from "react";

export type KbdSize = "sm" | "md";

export interface KbdProps extends HTMLAttributes<HTMLElement> {
  /** Size variant. "sm" for inline hints (9.5px); "md" default (11px). */
  size?: KbdSize;
}

const baseStyle: CSSProperties = {
  fontFamily: "var(--mono)",
  fontSize: 11,
  lineHeight: 1,
  padding: "2px 6px",
  borderRadius: 4,
  display: "inline-flex",
  alignItems: "center",
  whiteSpace: "nowrap",
  flexShrink: 0,
};

const sizeStyles: Record<KbdSize, CSSProperties> = {
  sm: { fontSize: 9.5, padding: "1px 5px" },
  md: {},
};

export const Kbd = forwardRef<HTMLElement, KbdProps>(function Kbd(
  { size = "md", children, style, className, ...rest },
  ref,
) {
  return (
    <kbd
      ref={ref}
      className={`ds-atom-kbd${className ? ` ${className}` : ""}`}
      style={{ ...baseStyle, ...sizeStyles[size], ...style }}
      {...rest}
    >
      {children}
    </kbd>
  );
});
```

TypeScript note: `<kbd>` has no `HTMLKbdElement` type in the DOM lib — use `HTMLElement` for the ref type. This is correct and will not cause a compile error.
  </action>
  <verify>
    <automated>npx vitest run src/inputs/Kbd --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ds-atom-kbd" src/inputs/Kbd/index.tsx` returns at least 1
    - `grep "forwardRef" src/inputs/Kbd/index.tsx` returns a match
    - `grep "HTMLElement" src/inputs/Kbd/index.tsx` returns a match (not HTMLKbdElement)
    - `grep "export.*Kbd\|export.*KbdProps\|export.*KbdSize" src/inputs/Kbd/index.tsx` returns 3 matches
    - vitest run exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 8 unit tests pass; index.tsx exports Kbd, KbdProps, KbdSize</done>
</task>

<task type="auto">
  <name>Task 2: Write Kbd Storybook stories (Kbd.stories.tsx)</name>
  <files>src/inputs/Kbd/Kbd.stories.tsx</files>
  <read_first>
    - src/inputs/Badge/Badge.stories.tsx (read full file — SRC map, Meta shape, DarkMode decorator)
    - src/inputs/Kbd/index.tsx (confirm exports before writing import)
  </read_first>
  <action>
Create src/inputs/Kbd/Kbd.stories.tsx following the Badge stories pattern exactly.

Story title: `"Inputs/Kbd"`. Layout: `"centered"`. Tags: `["autodocs"]`.

Required stories:

**Default** — `<Kbd>⌘K</Kbd>` (args: `{ children: "⌘K" }`)

**ModifierKeys** — render a flex-wrap grid of all modifier tokens, each as `<Kbd>`:
`⌘`, `⇧`, `⌥`, `⌃`, `Enter`, `Esc`, `Tab`, `Space`, `↑`, `↓`, `←`, `→`
Use `render: () => (...)` with a `<div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>`.

**Combinations** — three combos using `+` separators rendered as plain text between `<Kbd>` elements.
Use `render: () => (...)` with three rows in a flex-column div:
- Row 1: `<Kbd>⌘</Kbd><span style={{ fontSize: 11, color: "var(--ink-4)" }}>+</span><Kbd>K</Kbd>`
- Row 2: `<Kbd>⌘</Kbd><span>+</span><Kbd>⇧</Kbd><span>+</span><Kbd>D</Kbd>`
- Row 3: `<Kbd>⌘</Kbd><span>+</span><Kbd>N</Kbd>`
Wrap each row in `<div style={{ display: "flex", alignItems: "center", gap: 3 }}>`.

**SmSize** — same modifier keys grid as ModifierKeys but with `size="sm"` on all `<Kbd>` elements.
Use `render: () => (...)`.

**InlineInText** — `render: () => <p>Press <Kbd>ESC</Kbd> to dismiss the dialog.</p>`

**DarkMode** — copy the exact DarkMode decorator from Badge.stories.tsx (dark div with background "#1c1917", padding 16, borderRadius 8, overflowX "auto", minWidth 0). Inside the dark wrapper, render the ModifierKeys grid plus one Combinations row.

argTypes must include `{ className: { control: false }, style: { control: false } }`.

SRC constant map — include at least Default and InlineInText source strings.
  </action>
  <verify>
    <automated>grep -c "export const" src/inputs/Kbd/Kbd.stories.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const" src/inputs/Kbd/Kbd.stories.tsx` returns at least 6 story exports (Default, ModifierKeys, Combinations, SmSize, InlineInText, DarkMode)
    - `grep "DarkMode" src/inputs/Kbd/Kbd.stories.tsx` returns a match
    - `grep "\"Inputs/Kbd\"" src/inputs/Kbd/Kbd.stories.tsx` returns a match
    - `grep "autodocs" src/inputs/Kbd/Kbd.stories.tsx` returns a match
    - `grep "className.*control.*false" src/inputs/Kbd/Kbd.stories.tsx` returns a match
  </acceptance_criteria>
  <done>6 story exports present; DarkMode story uses dark decorator; Storybook can parse the file (no import errors)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component props | `children` and rest props from caller; no sanitization needed — React handles XSS via JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-017-02 | Information Disclosure | Kbd children | accept | Pure display element; renders whatever string caller provides; no PII processed |
</threat_model>

<verification>
```bash
npx vitest run src/inputs/Kbd --reporter=verbose
```
All tests green. Then:
```bash
grep -c "ds-atom-kbd" src/inputs/Kbd/index.tsx
grep "forwardRef<HTMLElement" src/inputs/Kbd/index.tsx
```
Both return matches.
</verification>

<success_criteria>
- src/inputs/Kbd/index.tsx exports Kbd, KbdProps, KbdSize
- All 8 unit tests pass
- src/inputs/Kbd/Kbd.stories.tsx has 6 stories including DarkMode
- TypeScript compiles (no HTMLKbdElement reference)
</success_criteria>

<output>
After completion, create `.planning/phases/017-simple-primitives/017-02-SUMMARY.md`
</output>
