---
phase: 017-simple-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/primitives.css
autonomous: true
requirements:
  - REQ-17-01
  - REQ-17-02
  - REQ-17-03

must_haves:
  truths:
    - "ds-atom-kbd CSS block exists in primitives.css with correct token values"
    - "ds-atom-relative-time CSS block exists in primitives.css with correct token values"
    - "ds-atom-pagination-* CSS blocks exist in primitives.css covering all 7 selector classes"
    - "Dark mode override rules exist for ds-atom-kbd and ds-atom-pagination-icbtn hover"
  artifacts:
    - path: "src/primitives.css"
      provides: "All Phase 17 component CSS classes"
      contains: "ds-atom-kbd"
  key_links:
    - from: "src/primitives.css"
      to: "src/inputs/Kbd/index.tsx"
      via: "className='ds-atom-kbd'"
      pattern: "ds-atom-kbd"
    - from: "src/primitives.css"
      to: "src/data-display/Pagination/index.tsx"
      via: "className='ds-atom-pagination*'"
      pattern: "ds-atom-pagination"
---

<objective>
Append all three ds-atom-* CSS blocks for Phase 17 to src/primitives.css.

Purpose: Components in Wave 2 render unstyled without these CSS blocks. CSS must land first so all Wave 2 plans can run in parallel without any styling debt.
Output: src/primitives.css extended with ds-atom-kbd, ds-atom-relative-time, and ds-atom-pagination-* blocks at end of file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/017-simple-primitives/017-UI-SPEC.md
@.planning/phases/017-simple-primitives/017-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append ds-atom-kbd CSS block to primitives.css</name>
  <files>src/primitives.css</files>
  <read_first>
    - src/primitives.css (read last 30 lines to confirm current end-of-file and that ds-atom-kbd is absent)
  </read_first>
  <action>
Read the last 30 lines of src/primitives.css to confirm the file ends at the sortable-overlay-ghost block (line ~4802). Verify `ds-atom-kbd` does not already exist (grep). Then append the following exact block after the final line of the file:

```css
/* ─── DS atom: Kbd ───────────────────────────────────────────────────
   Keyboard key label. Mono font, bordered chip. Token-driven dark mode. */
.ds-atom-kbd {
  font-family: var(--mono);
  font-size: 11px;
  line-height: 1;
  padding: 2px 6px;
  border-radius: 4px;
  border: 1px solid var(--rule);
  background: var(--cream-2);
  color: var(--ink-2);
  display: inline-flex;
  align-items: center;
  white-space: nowrap;
  box-shadow: 0 1px 0 var(--rule);
  flex-shrink: 0;
}
.dark .ds-atom-kbd {
  background: rgba(255, 255, 255, 0.08);
  border-color: rgba(255, 255, 255, 0.18);
  color: var(--ink);
}
```

Use the Edit tool to append — do NOT rewrite the entire file.
  </action>
  <verify>
    <automated>grep -c "ds-atom-kbd" /Users/temp/Documents/workspace/design-system/src/primitives.css</automated>
  </verify>
  <done>grep returns at least 2 matches (root selector + dark override rule)</done>
</task>

<task type="auto">
  <name>Task 2: Append ds-atom-relative-time and ds-atom-pagination-* CSS blocks to primitives.css</name>
  <files>src/primitives.css</files>
  <read_first>
    - src/primitives.css (read last 10 lines to confirm ds-atom-kbd block is present from Task 1)
  </read_first>
  <action>
After the ds-atom-kbd block appended in Task 1, append the following two blocks in order. Use the Edit tool.

Block 1 — ds-atom-relative-time:
```css
/* ─── DS atom: RelativeTime ─────────────────────────────────────────
   Inline relative timestamp. Mono font, cursor-default. Token-driven. */
.ds-atom-relative-time {
  font-family: var(--mono);
  font-size: 11px;
  font-weight: 700;
  color: var(--ink-2);
  cursor: default;
}
```
No dark-mode override needed — --ink-2 flips automatically via token system.

Block 2 — ds-atom-pagination-* (all 7 selector classes in one block):
```css
/* ─── DS atom: Pagination ────────────────────────────────────────────
   Page navigation: prev/next icon buttons + numbered page buttons + ellipsis. */
.ds-atom-pagination {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.ds-atom-pagination-list {
  display: flex;
  align-items: center;
  gap: 4px;
  list-style: none;
  margin: 0;
  padding: 0;
}
.ds-atom-pagination-icbtn {
  width: 28px;
  height: 28px;
  border-radius: 7px;
  border: 1px solid var(--rule);
  background: var(--g-bg);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: var(--ink-2);
  cursor: pointer;
  transition: all 0.15s;
  outline: none;
}
.ds-atom-pagination-icbtn:hover:not(:disabled) {
  background: var(--cream-2);
  border-color: rgba(0, 0, 0, 0.12);
}
.ds-atom-pagination-icbtn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}
.ds-atom-pagination-icbtn:focus-visible {
  outline: none;
  box-shadow: var(--focus-ring);
  border-color: var(--focus);
}
.dark .ds-atom-pagination-icbtn:hover:not(:disabled) {
  background: rgba(255, 255, 255, 0.08);
  border-color: var(--ink-4);
}
.ds-atom-pagination-btn {
  width: 28px;
  height: 28px;
  border-radius: 7px;
  border: 1px solid transparent;
  background: transparent;
  font-size: 12px;
  font-weight: 400;
  cursor: pointer;
  color: var(--ink-2);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  transition: all 0.12s;
  font-family: var(--font);
}
.ds-atom-pagination-btn:hover:not([aria-current="page"]):not(:disabled) {
  background: var(--cream-2);
}
.ds-atom-pagination-btn[aria-current="page"] {
  background: var(--ink);
  color: var(--cream);
  font-weight: 700;
  cursor: default;
}
.ds-atom-pagination-btn:focus-visible {
  outline: none;
  box-shadow: var(--focus-ring);
  border-color: var(--focus);
}
.ds-atom-pagination-ellipsis {
  width: 28px;
  height: 28px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  color: var(--ink-4);
  pointer-events: none;
  user-select: none;
}
.ds-atom-pagination-label {
  font-family: var(--mono);
  font-size: 10px;
  color: var(--ink-4);
  margin-left: 6px;
}
.ds-atom-pagination-count {
  font-family: var(--mono);
  font-size: 11px;
  font-weight: 700;
}
```
  </action>
  <verify>
    <automated>grep -c "ds-atom-relative-time\|ds-atom-pagination" /Users/temp/Documents/workspace/design-system/src/primitives.css</automated>
  </verify>
  <done>
    - grep returns at least 10 matches (1 relative-time + multiple pagination selectors)
    - All 7 pagination class names present: ds-atom-pagination, ds-atom-pagination-list, ds-atom-pagination-icbtn, ds-atom-pagination-btn, ds-atom-pagination-ellipsis, ds-atom-pagination-label, ds-atom-pagination-count
    - Verify: `grep "ds-atom-pagination-count\|ds-atom-pagination-label\|ds-atom-pagination-ellipsis" src/primitives.css` returns 3 lines
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS authoring | Planner-controlled static CSS; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-017-01 | Tampering | primitives.css | accept | Static CSS file; no runtime execution path; version-controlled |
</threat_model>

<verification>
After both tasks complete, run:
```bash
grep "ds-atom-kbd\|ds-atom-relative-time\|ds-atom-pagination" src/primitives.css | grep -v "^#" | wc -l
```
Expect >= 12 lines covering all three component blocks.

Also confirm the file has grown beyond 4802 lines:
```bash
wc -l src/primitives.css
```
Expect >= 4870 lines.
</verification>

<success_criteria>
- src/primitives.css contains ds-atom-kbd root + dark override (2 blocks)
- src/primitives.css contains ds-atom-relative-time (1 block, no dark override)
- src/primitives.css contains all 7 ds-atom-pagination-* classes plus 1 dark override
- File remains valid CSS (no syntax errors visible from grep structure check)
- No existing blocks were modified or removed
</success_criteria>

<output>
After completion, create `.planning/phases/017-simple-primitives/017-01-SUMMARY.md`
</output>
