---
phase: 023-datagrid
plan: 03
type: execute
wave: 3
depends_on:
  - "023-02"
files_modified:
  - src/data-display/DataGrid/DataGrid.stories.tsx
  - src/index.ts
autonomous: true
requirements:
  - REQ-23-01

must_haves:
  truths:
    - "DataGrid.stories.tsx exports at least 4 stories: Default, Sortable, WithSelection, DarkMode"
    - "src/index.ts exports DataGrid, DataGridColumn, DataGridRow, DataGridProps"
    - "tsc --noEmit exits 0"
    - "Full vitest suite: Phase 23 tests pass; pre-existing failures unchanged"
---

<objective>
Add Storybook stories for DataGrid, wire barrel export, run final phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: DataGrid stories</name>
  <files>src/data-display/DataGrid/DataGrid.stories.tsx</files>
  <read_first>
    - src/data-display/Table/Table.stories.tsx (table stories pattern)
    - src/data-display/DataGrid/index.tsx
  </read_first>
  <action>
Create `src/data-display/DataGrid/DataGrid.stories.tsx` with at least 4 stories:

- **Default** — Static dataset with 7 job-application rows. Demonstrates layout, badges, priority dots.
- **Sortable** — Same dataset, demonstrates sort indicators and sort callback wiring (controlled wrapper with useState).
- **WithSelection** — Demonstrates row selection + bulk-action bar appearing.
- **DarkMode** — Default decorator wrapped in `<div className="dark" style={{ background: "#1c1917", padding: 32 }}>`.

Use a `function DataGridDemo()` wrapper with useState for selection / sort / pagination state.

Set Meta:
- title: "Data Display/DataGrid"
- tags: ["autodocs"]
- layout: "padded"
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -c "^export const " src/data-display/DataGrid/DataGrid.stories.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exports at least 4 stories
    - DarkMode story has className="dark" decorator
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>4+ stories exported, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Barrel export + phase gate</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (find data-display section near Table / Pagination exports)
  </read_first>
  <action>
Insert into `src/index.ts` near the Pagination export:

```typescript
export {
  DataGrid,
  type DataGridProps,
  type DataGridColumn,
  type DataGridRow,
} from "./data-display/DataGrid";
```

Then run:
1. `npx tsc --noEmit` — must exit 0
2. `npx vitest run` — Phase 23 tests pass; pre-existing 3 failures unchanged
  </action>
  <verify>
    <automated>cd /Users/temp/Documents/workspace/design-system && grep -c "DataGrid" src/index.ts && npx tsc --noEmit && npx vitest run src/data-display/DataGrid 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/index.ts contains exports for DataGrid, DataGridProps, DataGridColumn, DataGridRow
    - tsc --noEmit exits 0
    - Phase 23 tests pass (12+)
  </acceptance_criteria>
  <done>DataGrid exported from package root. Phase gate green.</done>
</task>

</tasks>

<verification>
1. Stories file exports >= 4 names
2. `grep -c "DataGrid" src/index.ts` returns at least 4
3. `npx tsc --noEmit` exits 0
4. Phase 23 tests pass
</verification>

<success_criteria>
- 4+ stories shipped
- DataGrid + types importable from package root
- tsc clean, tests pass
</success_criteria>

<output>
Create `.planning/phases/023-datagrid/023-03-SUMMARY.md` after completion.
</output>
