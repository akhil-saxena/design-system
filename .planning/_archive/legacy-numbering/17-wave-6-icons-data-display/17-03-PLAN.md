---
phase: 17-wave-6-icons-data-display
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/SegmentedControl.tsx
  - src/SegmentedControl.stories.tsx
  - src/SegmentedControl.test.tsx
  - src/primitives.css
  - src/index.ts
autonomous: true
requirements:
  - DS-63
tags: [primitive, atom, radiogroup, aria]
user_setup: []

must_haves:
  truths:
    - "SegmentedControl renders 2-5 button options on a cream-2 pill background with amber-active state"
    - "Wrapper is role='radiogroup' with aria-label; each option is role='radio' with aria-checked + tabIndex"
    - "ArrowLeft/Right keys cycle selection (automatic activation per WAI-ARIA radiogroup)"
    - "Home/End keys jump to first/last option"
    - "Calendar (Plan 12) imports and uses SegmentedControl for view-mode toggle (month/week/day)"
    - "Dark-mode tokens applied — amber active, ink text on cream-2 in light, surface flip in dark"
    - ".ds-atom-segmented CSS block added to primitives.css with data-size variants"
  artifacts:
    - path: "src/SegmentedControl.tsx"
      provides: "Radiogroup-pattern segmented control primitive (DS-63)"
      exports: ["SegmentedControl", "type SegmentedControlProps", "type SegmentedOption"]
      min_lines: 80
    - path: "src/SegmentedControl.stories.tsx"
      provides: "Storybook stories — Default (3 options), TwoOptions, FiveOptions, Sizes, Disabled, DarkMode, Playground"
    - path: "src/SegmentedControl.test.tsx"
      provides: "Unit tests covering aria, keyboard nav, controlled value, edge cases"
      min_lines: 80
  key_links:
    - from: "src/index.ts"
      to: "src/SegmentedControl.tsx"
      via: "barrel re-export"
      pattern: "export \\{ SegmentedControl"
    - from: "src/SegmentedControl.tsx"
      to: "src/primitives.css"
      via: ".ds-atom-segmented class"
      pattern: "ds-atom-segmented"
---

<!-- Shared-barrel note: src/primitives.css and src/index.ts are append-only in this wave.
     Multiple Wave 2-4 plans add CSS blocks + barrel exports concurrently. Each plan APPENDS
     its block + export — no plan modifies another plan's existing entries. If a merge conflict
     occurs in CI, accept both blocks (concat). This convention is consistent with Waves 3-5
     in earlier phases (Phase 14 surfaces, 15 feedback, 16 inputs). -->

<objective>
DS-63 — Pill-shaped 2-to-5-option group built on the WAI-ARIA radiogroup pattern. **Calendar (Plan 12) depends on this primitive for its view-mode toggle (month/week/day) per D-17-20**, so DS-63 must land before DS-68. Distinct from Tabs (different ARIA family).

Purpose: a small visual-toggle control where exactly one option is active at a time, semantically equivalent to a styled radio group — used for view modes (Calendar), filter toggles, and 2-3 way switches throughout consumer apps.

Output: 1 primitive file + stories + tests, CSS block, barrel export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-wave-6-icons-data-display/17-CONTEXT.md
@.planning/phases/17-wave-6-icons-data-display/17-RESEARCH.md
@.planning/phases/17-wave-6-icons-data-display/17-PATTERNS.md
@.planning/phases/17-wave-6-icons-data-display/17-01-SUMMARY.md

# Analog files
@src/StarRating.tsx
@src/StarRating.stories.tsx
@src/StarRating.test.tsx
@design_handoff/design-system/ds-segmented.jsx
@src/primitives.css

<interfaces>
<!-- Component shape -->
```ts
export interface SegmentedOption<T extends string = string> {
  value: T;
  label: string;
  disabled?: boolean;
}

export interface SegmentedControlProps<T extends string = string> {
  options: SegmentedOption<T>[];
  value: T;
  onChange: (value: T) => void;
  size?: "sm" | "md" | "lg";
  disabled?: boolean;
  ariaLabel: string;
  className?: string;
  style?: React.CSSProperties;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build SegmentedControl primitive + stories + tests + CSS + barrel export</name>
  <files>src/SegmentedControl.tsx, src/SegmentedControl.stories.tsx, src/SegmentedControl.test.tsx, src/primitives.css, src/index.ts</files>
  <read_first>
    - src/StarRating.tsx (PRIMARY analog — radiogroup ARIA, button-array, data-active state, data-size, data-disabled, aria-checked per child) — read entire file
    - src/StarRating.stories.tsx (story file structure for atomic primitive — usage-audit JSDoc, Meta with title "Atoms/...", controlled wrapper)
    - src/StarRating.test.tsx (test file structure)
    - design_handoff/design-system/ds-segmented.jsx (visual reference — pill shape, amber active, sizes)
    - src/primitives.css (find the StarRating block at `.ds-atom-star` to mirror block-header style; find a sibling pill-shaped primitive like Chip for visual cues)
    - src/index.ts (barrel — append export)
    - 17-RESEARCH.md § "ARIA Patterns / SegmentedControl" lines 723-727 (radiogroup keyboard model)
    - 17-PATTERNS.md § "src/SegmentedControl.tsx" lines 240-273 (full pattern)
    - 17-PATTERNS.md § "Pattern 5: data-* attribute" lines 757-762 (variants without class concat)
    - 17-PATTERNS.md § "Pattern 8: Storybook usage-audit JSDoc" lines 789-793
  </read_first>
  <behavior>
    - Wrapper renders `<div role="radiogroup" aria-label={ariaLabel} className="ds-atom-segmented" data-size={size} data-disabled={disabled}>`.
    - Each option renders as `<button type="button" role="radio" aria-checked={value === opt.value} tabIndex={value === opt.value ? 0 : -1} onClick={() => onChange(opt.value)} disabled={disabled || opt.disabled} data-active={value === opt.value || undefined}>{opt.label}</button>`.
    - ArrowRight/ArrowDown moves focus + selection to next non-disabled option (wraps to first after last).
    - ArrowLeft/ArrowUp moves to previous (wraps to last after first).
    - Home jumps to first non-disabled option.
    - End jumps to last non-disabled option.
    - When all options are disabled, keyboard is no-op.
    - When `value` does not match any option, no option has `aria-checked=true` and tabIndex=0 falls back to the first non-disabled option (so the group is still focusable).
    - Sizes: sm (height 28, font 12), md (32, 13 — DEFAULT), lg (40, 14). Implemented via `data-size` CSS selector.
    - Pill-shape: `border-radius: 999px` on the wrapper, options have rounded inner corners; amber background on the active option.
    - Dark-mode flip: in `:root.dark` selector — wrapper bg flips to ink-2, active option amber stays the same (amber is brand-locked).
    - forwardRef to the wrapper div.
  </behavior>
  <action>
    1. Create `src/SegmentedControl.tsx`. File-header pattern: usage-audit JSDoc per Pattern 8, then `forwardRef<HTMLDivElement, SegmentedControlProps>` per Pattern 6.

    Skeleton:
    ```tsx
    /**
     * # Usage Audit — SegmentedControl (DS-63, D-17-20)
     *
     * Consumers (post v0.6):
     * - Calendar (DS-68) — view-mode toggle (month/week/day)
     * - Consumer apps — 2-5 way visual toggles (filter, mode, view)
     *
     * API:
     * - options: SegmentedOption[] (2-5 typical; 6+ should use Select)
     * - value: string (controlled)
     * - onChange: (value) => void
     * - size: "sm" | "md" | "lg" (default md)
     * - disabled: boolean (group-level)
     * - ariaLabel: string (REQUIRED — radiogroup needs accessible name)
     *
     * Implementation:
     * - WAI-ARIA radiogroup pattern with automatic activation
     * - Arrow keys cycle + select; Home/End jump to ends
     * - Pill-shaped wrapper with amber-active option
     * - data-size, data-disabled, data-active for CSS state
     */
    import { forwardRef, useCallback, useRef } from "react";

    export interface SegmentedOption<T extends string = string> {
      value: T;
      label: string;
      disabled?: boolean;
    }
    export interface SegmentedControlProps<T extends string = string> {
      options: SegmentedOption<T>[];
      value: T;
      onChange: (value: T) => void;
      size?: "sm" | "md" | "lg";
      disabled?: boolean;
      ariaLabel: string;
      className?: string;
      style?: React.CSSProperties;
    }

    function SegmentedControlInner<T extends string>({
      options, value, onChange, size = "md", disabled = false,
      ariaLabel, className, style,
    }: SegmentedControlProps<T>, ref: React.Ref<HTMLDivElement>) {
      const wrapperRef = useRef<HTMLDivElement | null>(null);
      const setRef = (node: HTMLDivElement | null) => {
        wrapperRef.current = node;
        if (typeof ref === "function") ref(node);
        else if (ref) (ref as React.MutableRefObject<HTMLDivElement | null>).current = node;
      };

      const enabledIndices = options
        .map((o, i) => (o.disabled ? -1 : i))
        .filter((i): i is number => i !== -1);
      const activeIndex = options.findIndex(o => o.value === value);

      const move = useCallback((delta: 1 | -1 | "home" | "end") => {
        if (disabled || enabledIndices.length === 0) return;
        const currentEnabledPos = enabledIndices.indexOf(activeIndex);
        let nextPos: number;
        if (delta === "home") nextPos = 0;
        else if (delta === "end") nextPos = enabledIndices.length - 1;
        else if (currentEnabledPos === -1) nextPos = 0;
        else nextPos = (currentEnabledPos + delta + enabledIndices.length) % enabledIndices.length;
        const nextOptionIndex = enabledIndices[nextPos]!;
        const nextOption = options[nextOptionIndex]!;
        onChange(nextOption.value);
        // Focus the newly-selected button
        const buttons = wrapperRef.current?.querySelectorAll<HTMLButtonElement>("button[role='radio']");
        buttons?.[nextOptionIndex]?.focus();
      }, [activeIndex, enabledIndices, options, onChange, disabled]);

      const onKeyDown = (e: React.KeyboardEvent<HTMLDivElement>) => {
        if (e.key === "ArrowRight" || e.key === "ArrowDown") { e.preventDefault(); move(1); }
        else if (e.key === "ArrowLeft" || e.key === "ArrowUp") { e.preventDefault(); move(-1); }
        else if (e.key === "Home") { e.preventDefault(); move("home"); }
        else if (e.key === "End") { e.preventDefault(); move("end"); }
      };

      // First non-disabled index is the fallback tabIndex=0 holder when value matches nothing
      const fallbackTabIndex = enabledIndices[0] ?? -1;

      return (
        <div
          ref={setRef}
          role="radiogroup"
          aria-label={ariaLabel}
          className={`ds-atom-segmented${className ? ` ${className}` : ""}`}
          style={style}
          data-size={size}
          data-disabled={disabled ? "true" : undefined}
          onKeyDown={onKeyDown}
        >
          {options.map((opt, i) => {
            const isActive = opt.value === value;
            const tabIndex = activeIndex >= 0
              ? (isActive ? 0 : -1)
              : (i === fallbackTabIndex ? 0 : -1);
            return (
              <button
                key={opt.value}
                type="button"
                role="radio"
                aria-checked={isActive}
                tabIndex={tabIndex}
                disabled={disabled || opt.disabled}
                data-active={isActive || undefined}
                onClick={() => { if (!disabled && !opt.disabled) onChange(opt.value); }}
                className="ds-atom-segmented-btn"
              >
                {opt.label}
              </button>
            );
          })}
        </div>
      );
    }

    export const SegmentedControl = forwardRef(SegmentedControlInner) as <T extends string>(
      props: SegmentedControlProps<T> & { ref?: React.Ref<HTMLDivElement> }
    ) => ReturnType<typeof SegmentedControlInner>;
    ```

    Note: forwardRef + generics requires the type-cast workaround above (`as <T extends string>(...) => ReturnType<...>`) — this is a known React + TypeScript limitation. Document inline.

    2. Create `src/SegmentedControl.stories.tsx`. Mirror StarRating.stories.tsx structure. Required stories:
    - `Default` — 3 options (e.g., Day/Week/Month), value=Week
    - `TwoOptions` — 2 options minimum
    - `FiveOptions` — 5 options maximum
    - `Sizes` — render three SegmentedControls with size="sm", "md", "lg" stacked
    - `Disabled` — entire group disabled
    - `WithDisabledOption` — group enabled but one option disabled
    - `DarkMode` — Default story with `:root.dark` parameter (use existing wave dark-mode story pattern from StarRating.stories.tsx if present; otherwise use Storybook's `parameters: { themes: { dark: true } }` or wrap in a dark-class div)
    - `Playground` — full controls

    3. Create `src/SegmentedControl.test.tsx`. Use Harness pattern from Select.test.tsx. Required tests:
    - renders all options as role=radio
    - aria-checked reflects value prop
    - clicking option fires onChange
    - ArrowRight cycles to next option (test by firing keydown, asserting onChange called with next value)
    - ArrowLeft cycles to previous (wraps from first to last)
    - Home jumps to first
    - End jumps to last
    - Skips disabled options on arrow keys
    - Disabled group: no keyboard activity, no clicks register
    - tabIndex=0 only on active option

    4. **CSS** — append to `src/primitives.css`. Block header per primitives.css convention. Recommended block:
    ```css
    /* ─── DS atom: SegmentedControl (DS-63) ─────────────────────────────────
       Pill-shaped radiogroup. Cream-2 wrapper, amber active option.
       Sizes via data-size; disabled state via data-disabled. */
    .ds-atom-segmented {
      display: inline-flex;
      align-items: stretch;
      gap: 2px;
      padding: 2px;
      background: var(--surface-2);
      border: 1px solid var(--rule);
      border-radius: 999px;
    }
    .ds-atom-segmented[data-disabled="true"] {
      opacity: 0.5;
      pointer-events: none;
    }
    .ds-atom-segmented-btn {
      appearance: none;
      background: transparent;
      border: 0;
      color: var(--ink-2);
      cursor: pointer;
      padding: 0 14px;
      border-radius: 999px;
      font-family: var(--font-body);
      font-weight: 500;
      transition: background-color 120ms ease, color 120ms ease;
    }
    .ds-atom-segmented-btn:hover:not([data-active]):not(:disabled) {
      color: var(--ink);
    }
    .ds-atom-segmented-btn:focus-visible {
      outline: 2px solid var(--amber);
      outline-offset: 2px;
    }
    .ds-atom-segmented-btn[data-active] {
      background: var(--amber);
      color: var(--ink-inverse, #000);
    }
    .ds-atom-segmented-btn:disabled {
      cursor: not-allowed;
      opacity: 0.5;
    }
    /* Sizes */
    .ds-atom-segmented[data-size="sm"] .ds-atom-segmented-btn { height: 28px; font-size: 12px; padding: 0 10px; }
    .ds-atom-segmented[data-size="md"] .ds-atom-segmented-btn { height: 32px; font-size: 13px; padding: 0 14px; }
    .ds-atom-segmented[data-size="lg"] .ds-atom-segmented-btn { height: 40px; font-size: 14px; padding: 0 18px; }
    /* Dark mode */
    :root.dark .ds-atom-segmented {
      background: var(--surface-2);
      border-color: var(--rule);
    }
    :root.dark .ds-atom-segmented-btn {
      color: var(--ink-2);
    }
    :root.dark .ds-atom-segmented-btn[data-active] {
      background: var(--amber);
      color: var(--ink-inverse, #000);
    }
    ```
    Verify token names match what's in `src/tokens.css` — read the file if uncertain. Use `--surface-2`, `--rule`, `--ink`, `--ink-2`, `--amber` as per existing conventions.

    5. **Barrel** — append to `src/index.ts`:
    ```ts
    export { SegmentedControl, type SegmentedControlProps, type SegmentedOption } from "./SegmentedControl";
    ```

    6. Run tests + typecheck.
  </action>
  <verify>
    <automated>npm run test:run -- SegmentedControl &amp;&amp; npm run typecheck &amp;&amp; grep -c "ds-atom-segmented" src/primitives.css &amp;&amp; grep -q "SegmentedControl" src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/SegmentedControl.tsx` and `.stories.tsx` and `.test.tsx` pass
    - `grep -c "role=\"radiogroup\"" src/SegmentedControl.tsx` returns at least 1
    - `grep -c "role=\"radio\"" src/SegmentedControl.tsx` returns at least 1
    - `grep -c "ArrowRight\\|ArrowLeft\\|Home\\|End" src/SegmentedControl.tsx` returns at least 4
    - `grep -v '^/\\*\\| \\*' src/primitives.css | grep -c "ds-atom-segmented"` returns at least 8
    - `grep -c ":root.dark .ds-atom-segmented" src/primitives.css` returns at least 1
    - `grep -c "SegmentedControl" src/index.ts` returns at least 1
    - `npm run test:run -- SegmentedControl` exits 0 with at least 8 tests passing
    - `npm run typecheck` exits 0
    - `npm run build` exits 0 (catches barrel/export issues)
  </acceptance_criteria>
  <done>SegmentedControl ships with full ARIA radiogroup pattern, 7 stories, 8+ unit tests, dark-mode CSS, exported from main barrel — Calendar (Plan 12) can now consume it.</done>
</task>

</tasks>

<threat_model>
N/A — Primitive renders structured props only. No user-input rendering paths beyond standard React JSX text rendering of option labels.
</threat_model>

<verification>
- All radio behaviors (aria-checked, tabIndex, keyboard cycling) verified by unit tests
- Dark mode story renders without cream-on-cream regression (manual visual check)
- Calendar (Plan 12) can import `{ SegmentedControl }` from main barrel
</verification>

<success_criteria>
1. role=radiogroup wrapper + role=radio options
2. ArrowLeft/Right/Home/End keyboard navigation
3. Three sizes via data-size
4. Pill shape, amber active, dark-mode tuned
5. Public export from src/index.ts
6. 8+ unit tests, 7 stories
</success_criteria>

<output>
After completion, create `.planning/phases/17-wave-6-icons-data-display/17-03-SUMMARY.md`.
</output>
