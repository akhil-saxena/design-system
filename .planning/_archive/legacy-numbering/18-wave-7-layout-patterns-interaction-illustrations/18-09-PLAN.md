---
id: 18-09
wave: 6
depends_on: [18-08]
primitive: release
requirements: [DS-71, DS-72, DS-73, DS-74, DS-75, DS-76, DS-77, DS-78, DS-79, DS-80, DS-81]
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
  - src/index.ts
autonomous: false

must_haves:
  truths:
    - "package.json version is 1.0.0"
    - "CHANGELOG.md has a ## [1.0.0] section listing all DS-71..81 primitives"
    - "README.md import map shows all three subpath exports (/, /icons, /hooks, /illustrations)"
    - "npm run build exits 0 with all 4 tsup entries"
    - "npm test exits 0 with all tests passing (target: ≥800 tests)"
    - "dist/index.d.ts exports AppShell, AppBar, Footer, Wizard, PasswordStrength, FieldError, FormErrorSummary, Coachmark, InlineEdit, SearchAndFilters, Presence (via Avatar), Sortable"
    - "dist/illustrations/index.d.ts exports all 24 illustration components"
  artifacts:
    - path: "package.json"
      provides: "version 1.0.0"
      contains: "\"version\": \"1.0.0\""
    - path: "CHANGELOG.md"
      provides: "v1.0.0 release notes"
      contains: "1.0.0"
    - path: "README.md"
      provides: "full library documentation"
      contains: "/illustrations"
  key_links:
    - from: "package.json"
      to: "dist/"
      via: "npm run build"
      pattern: "\"version\": \"1.0.0\""
---

<objective>
v1.0.0 release: version bump, CHANGELOG, README update, final build + test verification, cumulative visual baselines, git tag.

Purpose: The culminating plan — cut v1.0.0 of @akhil-saxena/design-system with all 58 primitives (35 from earlier phases + 11 from Phase 17 + 12 from Phase 18) and the three subpath exports.

Output: package.json 1.0.0, CHANGELOG.md, updated README.md, clean build, all tests green, git tag v1.0.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/index.ts

<interfaces>
<!-- Current package.json version: "0.6.0" → bump to "1.0.0" -->
<!-- Current exports stanza (4 entries after 18-00): -->
<!--   ".", "./hooks", "./icons", "./illustrations" + 3 CSS files -->

<!-- CHANGELOG.md conventions from prior releases: -->
<!--
## [0.6.0] — 2026-04-29
### Added
- DS-60 Icons (/icons subpath, tree-shakeable Lucide wrappers)
- DS-61..70 data display primitives (Table, Tabs, SegmentedControl, Accordion, Carousel, Timeline, InfiniteList, Calendar, Breadcrumbs, RichText)
### Internal
- DSDropdown internal extracted
...
-->

<!-- README.md must include: -->
<!--
  - Short description
  - Install section: npm install @akhil-saxena/design-system
  - CSS imports (3 layers)
  - Import map: main, /hooks, /icons, /illustrations
  - Primitive count (58 total)
  - Migration notes (0.x → 1.0)
  - Link to Storybook (if deployed)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump + CHANGELOG + README</name>
  <files>package.json, CHANGELOG.md, README.md</files>
  <read_first>
    - package.json — current version + deps to confirm all @dnd-kit present
    - CHANGELOG.md — read full file to understand format; append new section at top
    - README.md — read full file; update primitive count + imports section
    - src/index.ts — count current exports to confirm 58 primitives total
  </read_first>
  <action>
    1. Edit `package.json`: change `"version": "0.6.0"` to `"version": "1.0.0"`.
       Confirm @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities all present in `dependencies`.

    2. Edit `CHANGELOG.md`: prepend a new section at the very top (after the `# Changelog` header):

       ```markdown
       ## [1.0.0] — 2026-05-02

       ### Wave 7: Layout, Patterns, Interaction + Illustrations — v1.0.0

       **Phase 18 — 12 new primitives (DS-71..82)**

       #### Layout Shell
       - **DS-71 AppShell** — CSS Grid layout shell; sidebar collapsible to 48px icon rail; localStorage persistence; props-based slots (sidebar / topbar / main / footer)
       - **DS-72 AppBar** — Standalone topbar primitive; 4 variants (minimal / withSearch / default / centered); scrolled blur effect
       - **DS-73 Footer** — compact (1-line) and expanded (4-column) variants

       #### Patterns
       - **DS-74 Wizard** — Multi-step form scaffold; horizontal + vertical stepper; ProgressBar integration; per-step validate() gate; useFocusTrap boundary
       - **DS-75 FormValidation** — PasswordStrength (4-segment score bar), FieldError, FormErrorSummary composable helpers
       - **DS-76 Coachmark** — First-run hint anchored to target via Popover; dismissible; localStorage persist; step counter dots

       #### Interaction
       - **DS-77 InlineEdit** — Click-to-edit text/textarea; optimistic save + error recovery; Escape-to-cancel
       - **DS-78 SearchAndFilters** — Search input + DSDropdown autocomplete + Chip filter tokens + clear-all
       - **DS-79 Presence** — Avatar extended with 4-corner presencePosition prop (top-right / bottom-right / top-left / bottom-left)

       #### Drag & Drop
       - **DS-80 Sortable** — @dnd-kit/core + @dnd-kit/sortable; list reorder + cross-list; keyboard Space/Arrow; 1px amber drop indicator; prefers-reduced-motion respected

       #### Illustrations subpath
       - **DS-81 Illustrations** — `/illustrations` subpath export; 24 named SVG React components; tree-shakeable; dark-mode token-flip; width+height props (default 120×120)

       #### Infrastructure
       - Added `@dnd-kit/core@6.3.1`, `@dnd-kit/sortable@10.0.0`, `@dnd-kit/utilities@3.2.2` as production dependencies
       - Added `@akhil-saxena/design-system/illustrations` subpath export

       ### Migration from 0.x

       No breaking changes. All exports are additive. The `/illustrations` subpath is new — no existing import paths changed.

       ```typescript
       // New in v1.0.0:
       import { AppShell, AppBar, Footer } from '@akhil-saxena/design-system';
       import { Wizard, Coachmark, InlineEdit, SearchAndFilters } from '@akhil-saxena/design-system';
       import { PasswordStrength, FieldError, FormErrorSummary } from '@akhil-saxena/design-system';
       import { Sortable, SortableItem } from '@akhil-saxena/design-system';
       import { EmptyBox, MailSent, Rocket } from '@akhil-saxena/design-system/illustrations';
       ```
       ```

    3. Edit `README.md`: Update/replace the existing content with:
       - **Header**: `# @akhil-saxena/design-system`
       - **Description**: "Accessible React primitive library — 58 primitives, full dark mode, cream+ink+amber design language. Extracted from JobDash v1.0."
       - **Install**: `npm install @akhil-saxena/design-system`
       - **CSS setup** (must import in consuming app):
         ```js
         import '@akhil-saxena/design-system/tokens.css';
         import '@akhil-saxena/design-system/primitives.css';
         import '@akhil-saxena/design-system/utilities.css';
         ```
       - **Import map** section:
         ```ts
         // Main primitives (58 total)
         import { Button, Modal, Table, AppShell, Sortable, ... } from '@akhil-saxena/design-system';
         // Hooks
         import { useFocusTrap, useClickOutside, useReducedMotion, useTokens } from '@akhil-saxena/design-system/hooks';
         // Icons (tree-shakeable lucide wrappers, 1.5px stroke, 20×20)
         import { ChevronDown, Search, Check } from '@akhil-saxena/design-system/icons';
         // Illustrations (24 spot SVGs, 120×120 default)
         import { EmptyBox, MailSent, Rocket, IllustrationSearch } from '@akhil-saxena/design-system/illustrations';
         ```
       - **Primitive index table**: list all DS-NN primitives by category with one-line description
       - **Dark mode**: explain `.dark` class on ancestor element
       - **Version**: v1.0.0
  </action>
  <verify>
    <automated>node -e "const v=require('./package.json').version; console.log(v === '1.0.0' ? 'PASS' : 'FAIL: ' + v)"</automated>
  </verify>
  <done>package.json version is 1.0.0. CHANGELOG.md has [1.0.0] section at top. README.md has /illustrations import example.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final build + test verification checkpoint</name>
  <what-built>
    All Phase 18 primitives implemented (DS-71..81), package version bumped to 1.0.0, CHANGELOG and README updated.
    Claude will run the full build + test suite before this checkpoint.
  </what-built>
  <pre-checkpoint-automation>
    Run these commands before pausing:

    ```bash
    # 1. Full build
    npm run build 2>&1 | tail -20

    # 2. Full test suite
    npm test -- --run 2>&1 | tail -20

    # 3. TypeScript check
    npm run typecheck 2>&1 | grep error | head -20

    # 4. Confirm all DS-71..81 in barrel
    grep -E "AppShell|AppBar|Footer|Wizard|PasswordStrength|FieldError|FormErrorSummary|Coachmark|InlineEdit|SearchAndFilters|Sortable|AvatarPresencePosition" src/index.ts

    # 5. Confirm illustrations
    grep -c "export function" src/illustrations/index.ts
    # expects: 24

    # 6. Confirm dist types
    ls dist/index.d.ts dist/illustrations/index.d.ts dist/icons/index.d.ts dist/hooks/index.d.ts
    ```
  </pre-checkpoint-automation>
  <how-to-verify>
    1. Run `npm run storybook` and visually inspect 3 new primitives:
       - AppShell: sidebar collapses to icon rail; localStorage persists collapse across reload
       - Sortable: drag an item; keyboard Space to lift → ArrowDown/Up → Space to drop
       - Illustrations: Gallery story shows all 24 in light + dark mode (verify no white patches in dark)

    2. Check the test count output — should be ≥800 tests.

    3. Check `dist/index.d.ts` exports (search for "AppShell" + "Sortable" + "Wizard")

    4. Verify the Illustrations dark mode — open dark story and confirm no `fill="#fff"` white shapes visible on dark background.
  </how-to-verify>
  <resume-signal>Type "approved" when visual checks pass, or describe specific issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Cumulative visual baselines + git tag</name>
  <files>tests/visual/storybook.spec.ts-snapshots/ (auto-generated)</files>
  <read_first>
    - tests/visual/storybook.spec.ts — visual regression test file (do not modify the spec itself)
    - .planning/phases/17-icons-data-display/17-14-SUMMARY.md — v0.6.0 release pattern to follow
  </read_first>
  <action>
    1. Update visual baselines to include all Phase 18 stories:
       ```bash
       npm run test:visual:update
       ```
       This regenerates all Playwright snapshots (including new AppShell, AppBar, Footer, Wizard, Coachmark, InlineEdit, SearchAndFilters, Presence, Sortable, Illustrations stories). Accept all new baselines.

    2. Run the visual suite once more to confirm it passes:
       ```bash
       npm run test:visual
       ```

    3. Make the release commit:
       ```bash
       git add -A
       git commit -m "chore(release): v1.0.0 — Wave 7 (Layout + Patterns + Interaction + Illustrations) — 12 primitives DS-71..81"
       ```

    4. Tag the release:
       ```bash
       git tag v1.0.0
       ```

    5. Confirm tag created:
       ```bash
       git tag --list | grep v1.0.0
       ```

    Note: Do NOT push tag or package until the user explicitly requests it (GitHub Packages publish requires separate auth). The tag is created locally; push is a separate step.
  </action>
  <verify>
    <automated>git tag --list | grep v1.0.0 && echo "TAG OK" || echo "TAG MISSING"</automated>
  </verify>
  <done>git tag v1.0.0 exists locally. Release commit created with all Phase 18 files. Visual baselines updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm publish | Package published to GitHub Packages (gated — user must run separately) |
| git tag | Local tag; push is user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09-01 | Tampering | CHANGELOG.md content | accept | Documentation file; no security impact |
| T-18-09-02 | Information Disclosure | dist/index.d.ts | accept | Type declarations are public library API — no secrets |
</threat_model>

<verification>
```bash
# Version is 1.0.0
node -e "console.log(require('./package.json').version)"
# expects: 1.0.0

# Build clean with all 4 entries
npm run build 2>&1 | tail -5

# Full test suite
npm test -- --run 2>&1 | tail -5
# expects: all tests pass, count >= 800

# All Phase 18 primitives in barrel
grep -E "AppShell|AppBar|Footer|Wizard|FormValidation|Coachmark|InlineEdit|SearchAndFilters|Sortable" src/index.ts | wc -l
# expects: >= 8

# Illustrations barrel
grep "export function" src/illustrations/index.ts | wc -l
# expects: 24

# dist types exist
ls dist/index.d.ts dist/illustrations/index.d.ts dist/icons/index.d.ts dist/hooks/index.d.ts

# git tag
git tag --list | grep v1.0.0

# CHANGELOG has [1.0.0]
grep -c "1.0.0" CHANGELOG.md
# expects: >= 1
```
</verification>

<success_criteria>
- package.json version = "1.0.0"
- npm run build exits 0 (4 tsup entries: index, hooks, icons, illustrations)
- npm test exits 0 (≥800 tests)
- CHANGELOG.md [1.0.0] section lists all DS-71..81 primitives
- README.md shows all 4 import paths including /illustrations
- dist/ contains all 4 subpath outputs: index.d.ts, hooks/index.d.ts, icons/index.d.ts, illustrations/index.d.ts
- git tag v1.0.0 created
- No `// TODO` markers in any Phase 18 src/*.tsx files
</success_criteria>

<output>
After completion, create `.planning/phases/18-wave-7-layout-patterns-interaction-illustrations/18-09-SUMMARY.md`
</output>
